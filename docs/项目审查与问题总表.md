# 项目审查与问题总表（施工工单 · 第 3 版）

> **第 3 版为什么存在**：前两版的"现状 / 要改成"多数继承自更早的审查结论（二手信息），当时只核对了**文件路径与方法名**、没有核对**结论本身**；而仓库在这期间被持续修改，于是出现"位置对、结论错"——第 2 轮复核推翻了 6 条结论（集中登记在 §10.2）。
> **本版的做法**：每条可施工任务的"问题"后面**直接贴出当前代码原文**，并给每条标注可靠性等级。
> **第 3 轮复核**：① 把剩下标 `[复核]` 的条目**逐条打开读过**（A1/A2/A7/A8、B4①②③、B5②、B8、B9、C4、C5、A10）—— **描述全部属实，0 处不符**；② **首次真正跑了构建与测试**：`dotnet build -c Release` **0 error / 0 warning**、`dotnet test` **50 通过 / 0 失败**；③ 转去读此前**从未通读**的 `AttendanceBackgroundService.cs` 与 `UserService.cs`，**新发现 2 条缺陷 + 1 条待确认**：**B10**（二级审批两个节点可能同一人）、**B11**（旷工标记没有补跑机制）、**§8-17**（停用中的设备会被静默解绑）；并补全了 §9 关于"删用户门槛"的描述。
> **第 8 轮（全量通读收尾）**：把此前**未逐行读过的代码全部读完**——`Program.cs`（全 378 行）、`AttendanceService.cs`（全 1869 行，分 7 段读完）、`ApprovalService.cs`（`:196-753` 本轮读完，`:1-196` 前几轮已按登记条目读过）、`UserService.cs`（本轮读 `:1-115` 与 `:542-666`；中段 `:116-541` 只在前几轮按已登记条目（B40/B41/B44/B45/B46/B47/B49）**定点**读过，**不是**逐行通读——如需再确认这一段请另开一轮）、`ZKDeviceController.cs`（全 430 行）、`ZKDeviceSyncService.cs`（全 403 行），以及 `MonthlyReport`/`PunchAdjust`/`HolidayManage`/`MyCalendar`/`MyRecord`/`Board`/`Publish` 等页面 code-behind、`AttendanceDbContext` 索引区、`AppSettingsOptions`/`ZKDeviceOptions`、`PrivateFileStorage`/`ImageValidationHelper`，并对全部测试/DTO/接口/实体/迁移做了一轮覆盖性通读。**`.cshtml` 视图的渲染路径（除 `UserManage.cshtml`、`ShiftManage.cshtml:1-260`、`_Layout.cshtml`、`PendingApproval.cshtml`）仍是最薄弱的一层。** 新增 **§10.5（B73~B92：10 中 + 10 低）** 与 **§10.7（B93~B107：3 中 + 12 低；其中 12 条低为并行通读产出、主审未逐条复核，已标 `[转述]`）**，引文核对结果记在 **§10.6**（1 处过期描述 + 9 处行号漂移，内容均属实）。**本轮没有发现新的"高"级缺陷。**
> **第 9 轮（按你的要求整理）**：把历次审查出的问题**逐条统计**（见 **§0.7**），并把**已修复/已完成/已作废的条目从 §1~§6、§8、§11 的表格中删除**（删除清单落在 §10.1 备查）。整理依据是 `git log` 的实际提交内容 + 逐条核对当前代码，**不是**照旧结论。整理后：原工单 55 条 → **剩 26 条待施工**；第 7/8 轮新增 93 条（B15~B107）**全部仍未修**；另 §7 有 9 项需人工在生产/真机确认、§8 剩 8 项待你决策。
> **本轮仍未做的**：没有运行程序、没有连数据库、没有跑真机设备协议 —— 全部结论仍是**静态阅读**得出；B76/B77 两条的运行期语义本地无法证实，已在表中显式标注"需实测"。
> **最重要的使用规则**：**引文与代码不一致 ⇒ 该条已过期，停下并报告，不要照文档改。**
> 基准：`HEAD = 37bfafe`（2026-09-22）。**工作区已无未提交的代码改动**（`git status` 只剩本文档本身）。定位以「文件 + 方法名」为准，行号仅供导航。

---

## 0. 使用说明（先读完这一节）

### 0.1 三条硬规则

**规则 1：先核对引文，再动手。**
每条可施工任务里都有一段 `当前代码`（从仓库复制的原文）。开工前打开对应文件确认那几行还在：

- 引文一致 → 按"要改成"施工
- **引文不一致 / 找不到 → 停下，不要改**，在 §11 登记 `❓ 引文不符`，写清你实际看到什么
  （这说明该条已过期或被改过 —— 第 2 轮就是这么抓出 6 条错结论的）

**规则 2：只做"可施工"的条目。** 标 `🟦 先复验` 的先判断现状再决定改不改（§1 已核完大部分）。
**规则 3：不确定就问，不要猜。** 尤其 A1 / A4 / A11（会打断生产设备）、§8（工资口径）、C6（数据库结构）。

### 0.2 可靠性标签（每条任务头部都标）

| 标签 | 含义 | 怎么用 |
|---|---|---|
| `[亲验]` | 本版编写时我读过该处当前代码并贴了原文 | 可当事实依据 |
| `[复核]` | 并行审查给了 `文件:行` + 代码片段，我已 grep 复核关键行确实存在 | 可施工，但引文核对这一步不能省 |
| `[未亲验]` | 只有历史结论、**没有**当前代码证据 | **禁止直接施工**，先复验 |

**可靠度边界（务必知道：本文档不保证"全部正确"）**：

1. `[亲验]` 保证的是**"引文与当前代码一致"**，**不保证"运行时行为如我所述"** —— 我全程只做静态阅读（外加 `dotnet build` / `dotnet test` 各跑一次），**没有运行过程序、没有造数据验证过实际输出**
2. 凡 **"全库 / 全仓 / 全工程 无 X"** 的结论都是 grep 推出来的：grep 只能证明"**这个模式没匹配到**"，不等于"不存在"。**这个坑已经踩过**：第 4 轮自检换个更宽的模式，就抓到本文档 2 处错（`\.Migrate\(` 漏掉 `MigrateAsync(`；`MustChangePassword` 数成 4 处、实为 5 处）——两处均已修正
3. §8 里的**数字**（如半天假的"请假 4.5h / 上限 3.5h"）是我按公式**手算的推算**，依赖"班次 08:30–12:00 + 13:00–17:30、标准 8h、午休 60 分钟"这组假设；换班次配置数字就不同
4. §7 的 9 项**明确无法在本机验证**（需生产/真机）
5. **时效性**：工作区有 24 个未提交文件、代码随时会变；我读的是**快照**。任何一条都可能因下一次改动而过期 —— **这正是规则 1 存在的原因**：这份文档的正确性，最终由"执行者动手前核对引文"这一动作兜底，而不是靠一次性保证

### 0.3 十条通用硬约束（违反即不合格）

1. **禁止全局重命名、格式化、批量替换** —— 会产生上万行 diff，审查无法进行。只改与任务直接相关的行。
2. **一次只做一个任务编号**；做完立刻在 §11 回执表登记。
3. **文件红线**：`docs/更新日志.md` 只能在最上方追加新条目；`docs/嵌入管理员AGENT_实现方案.md` 不许动；`appsettings.json` / `appsettings.Production.json` 是**未入库的明文密钥文件**（`.gitignore:12-13` 排除），不许提交、不许把内容写进日志；`migrate.sql` 只许用 `dotnet ef migrations script --idempotent` 重新生成，不许手改。
4. **改数据结构必须三件套同步**：迁移文件 + `Migrations/AttendanceDbContextModelSnapshot.cs` + `migrate.sql`。生产**不会**自动迁移。
5. **新增迁移禁止** `DROP TABLE`、删除真实业务数据、先删后建索引等不可逆操作；必须可重复执行且有可用的 `Down()`。
6. **不许做**：§8 待决策项、§9 暂缓项、任何"顺手重构"。
7. **时间体系不许改**：不要引入 UTC 归一、不要改 `DateTime.Now` 用法。
8. **已验证安全的地方不要"修"**：见 §0.4。
9. **用户能感知的改动**：按 `CLAUDE.md` 在 `docs/更新日志.md` 顶部追加一条，并执行
   `powershell -ExecutionPolicy Bypass -File tools/update_changelog_docx.ps1`。
10. **不要 `git commit`**：只改工作区。（若用户明确要求提交：untracked 文件必须显式 `git add`。）

### 0.4 请勿改动清单（这些都已核实，改了会坏）

| 位置 | 引文 / 事实 |
|---|---|
| `Pages/Notice/Board.cshtml` | 已用 `textContent` + `JsonSerializer.Serialize` 输出（`:67`、`:75-77`），不要改成 `innerHTML`。**注意：看板/公告页在 `Pages/Notice/` 下，不在 `Pages/Attendance/`** |
| `Pages/Approval/PendingApproval.cshtml` | `${d.no}`（`:350`）/`${d.type}`（`:351`）/日期（`:358-382`）**故意不转义**（数据源是受控枚举与日期）；同页 `:352`/`:381` 对姓名/理由用了 `escapeHtml`，是有意区别 |
| 补卡 / 手动补卡里显式设置 `ClockOutTime` | `AttendanceService.cs:1036-1037`、`:1238-1239` —— "由操作人决定下班时间"的刻意设计，**不要**改成"取更晚" |
| `Helpers/ExcelExportHelper.cs` 的字符串写入 | `:631-641` 走 POI `SetCellValue(string)`＝`CellType.String`，xlsx 无 `<f>` 元素。**不要**加 `'` 前导单引号（会变成可见字符，姓名变 `'张三`）。以后若新增 CSV 导出，只在 CSV 那条路径转义 |
| `Program.cs:42` `mysql.EnableRetryOnFailure(3);` | 与手动事务冲突（曾是生产 P0：审批全部失败）。要用手动事务**必须**用 `CreateExecutionStrategy().ExecuteAsync` 包裹（参考 `ApprovalService.cs:216-219`） |
| `AttendanceService.ComputeWorkHours:1557-1568` | `if (rawMinutes > 6 * 60) minutes -= lunchBreak;` —— "多干一分钟反而少算一小时"的悬崖是**业务认可**的"宁少勿多"，不要"修正" |
| **`IsGroupInScopeAsync`（5 份）里的 `Any`** | `AdminController.cs:338` / `GroupManage.cshtml.cs:172` 等，管的是"**能不能看到**"，`Count == 0 → true`（零部门共用组**对所有人可见**）。**绝对不要把 `Any` 改成 `All`** —— 会破坏"零部门组可见"。详见 A6 |
| `GenerateMonthlySummaryAsync` 已有的批量预载 | `AttendanceService.cs:698`（`recordsByUser`）、`:717`（`groupsById`）—— 已经优化过，**不要**按 C2 旧结论再去"消除 N+1" |
| `Services/Implementations/DeptScopeService` | 全系统数据范围的唯一口径，不要在页面/控制器里另写一套 |

### 0.5 完成定义（DoD）

**基线（2026-09-21 实测，开工前请先自己跑一遍确认起点一致）**：

| 命令 | 实测结果 |
|---|---|
| `dotnet build -c Release` | ✅ **0 error / 0 warning** |
| `dotnet test AttendanceSystem.Tests` | ✅ **通过 50 / 失败 0 / 跳过 0** |

- `dotnet build -c Release` 必须 0 error、不新增 warning
  （环境：SDK 10.0.302 + **.NET 8.0.29 运行时已装**，工程目标 `net8.0`）
- `dotnet test AttendanceSystem.Tests` 必须全绿
  **必须带项目名单独跑**：`AttendanceSystem.csproj` 的 `DefaultItemExcludes` 含 `AttendanceSystem.Tests\**`，`AttendanceSystem.slnx` 也不含它，不带项目名跑不到测试。
  **⚠️ 受限沙箱下会跑不起来**：实测测试宿主因"无法访问父进程句柄"报 `Win32Exception (5) 拒绝访问` 而中止（**这是环境限制，不是代码失败**），需要更宽的进程权限才能执行。
- 涉及数据库的：迁移三件套一致，并在测试库跑一次 `Up` 和 `Down`
- 已在 §11 回执表登记

### 0.6 施工顺序

**先做 §1（复验）** → 再 A → B → C → D → E。§1 里多数条目**已核完是"已修"**，登记即可关闭，不要写代码。

---

### 0.7 问题统计总览（第 9 轮整理：**已修项已从各表删除**）

**整理依据**：`git log` 各提交的实际内容 + 逐条核对当前代码（**不是**照旧结论）。凡已修复的条目**已从 §1~§6、§8、§11 的表格中删除**，只在 §10.1 留一行"已关闭编号 + 提交号"，避免重复修。工作区当前**没有未提交的代码**（`git status` 只有本文档本身）。

| 来源 | 发现 | 已修（已删除） | 仍待施工/决策 |
|---|---|---|---|
| V 批复验（§1） | 12 | **12**（V1~V12 全部关闭；其中 V10/V11 判为"合理设计非缺陷"） | 0 |
| A 批安全（§2） | 11 | **3**（A5、A6、A9） | 8（A1、A2、A3、A4、A7、A8、A10、A11） |
| B 批口径（§3） | 14 | **12**（B1~B6、B9~B14） | 2（**B7**：① 死字段仍在、② PunchCard 已删；**B8**：格式校验已收敛、范围判定未收敛） |
| C 批性能（§4） | 7 | **1**（C3，随 B3 一起完成） | 6（C1、C2、C4、C5、C6、C7） |
| D 批功能（§5） | 1 | **1**（D1 文员计入正式工） | 0 |
| E 批测试（§6） | 10 | 0 | 10（E1~E10） |
| **小计（原工单）** | **55** | **29** | **26** |
| 第 7 轮新增（§10.4，B15~B72） | 58（高 8 / 中 20 / 低 30） | 0 | 58 |
| 第 8 轮新增（§10.5，B73~B92） | 20（中 10 / 低 10） | 0 | 20 |
| 第 8 轮并行通读（§10.7，B93~B107） | 15（中 3 / 低 12） | 0 | 15 |
| **小计（新增问题）** | **93** | 0 | **93** |
| **合计** | **148** | **29** | **119** |

**剩余 119 条的严重度分布**：🔴 高 **8**（全部在 §10.4）｜🟡 中 **33**（§10.4 的 20 + §10.5 的 10 + §10.7 的 3）｜🟢 低 **52**（§10.4 的 30 + §10.5 的 10 + §10.7 的 12）｜另含上表 A/B/C 的 16 条与 E 批 10 条测试缺口（已计入 26 条小计，不再重复计数）。

**另两类不是"代码问题"的清单**：

| 清单 | 原数量 | 已落实/作废（已删除） | 剩余 |
|---|---|---|---|
| §7 只能在生产/真机确认 | 9 | 0 | **9** |
| §8 待用户决策 | 17 | **9**（#1 按备选 B 落实、#2、#5、#9 作废、#13 作废、#14、#15、#16、#17） | **8**（#3、#4、#6、#7、#8、#10、#11、#12） |
| §9 暂缓（禁止施工） | 6 条 | 0 | 6 |

**"已修"的提交依据**（逐条对应已删除的编号）：

| 提交 | 日期 | 覆盖的已修条目 |
|---|---|---|
| `37bfafe` | 2026-09-22 | §8-15、**B14** |
| `166ae18` | 2026-09-21 | **B12、B13** |
| `c89b5ad` | 2026-09-21 | **D1、B2、B3+C3、B4、B5、B6、B8** |
| `78e8bfa` | 2026-09-21 | **A5、A9、B1、B7②、B9、B10、B11**、§8-2、§8-5、§8-14、§8-16、§8-17 |
| `7404015` | 2026-09-21 | 工时/出勤口径一批、出勤天数公式统一、设备分公司隔离取消（对应 §1.1 的 V 批若干项） |
| `9784347` | 2026-09-18 | 午间窗口去重、审批事务与重试策略、加班重复提交、请假天数边界、半天假出勤重复计算 |

> **⚠️ 仍需人工确认的工资口径**（与已修项直接相关，别漏）：生产 `__EFMigrationsHistory` 必须含 `20260918075907_ChangeActualWorkdaysToDecimal`，否则 `ActualWorkdays` 还是 `int`，21.5 会被截断 → **直接影响工资**（见 §7-4）。

---

## 1. 复验结果（🟦 先看这里，能省一半工作量）

> **第 9 轮整理：本节原有的 V 批明细表（V1~V12）已全部关闭并从表中删除**，逐个编号与提交依据见 **§0.7** 与 **§10.1**。**V 批已无待办**，施工者不必在此停留。

**只保留两条"已知正确、不要改"的结论**（防止后人按旧怀疑改坏）：

- **V10 `ApproverCoversApplicantAsync` 对不受限账号恒真** —— `if (scopeId is null) return true;`：总部本就管所有人，**正确**；受限账号走"申请人部门祖先链"判定，也正确。
- **V11 加班回写按天累加 `+=`** —— **正确**：提交侧已拦时段重叠（V12），审批节点"原子认领"（`ApprovalService` 里的 `ExecuteUpdateAsync` 条件更新）也防住了重复处理；不重叠的两单累加本身就对。**但**：`AttendanceService.UpdateAttendanceAfterApprovalAsync` 的累加在"执行策略重试"下的幂等性仍未实测 → 见 **§10.7 的 B77**。

---

## 2. A 批：安全与越权

### A1 设备 SN 字符集白名单 + 文件名清洗 `P0` `[亲验]`
- **位置**：`Controllers/ZKDeviceController.cs` `SaveAttPhotoAsync`；登记入口 `Pages/Admin/ZKDeviceManage.cshtml.cs:40-44`
- **当前代码**（`ZKDeviceController.cs:413-414`）：
  ```csharp
  var fileName = $"{sn}_{DateTime.Now:HHmmss}_{Guid.NewGuid():N}.jpg";
  // → Path.Combine(dir, fileName)，无 Path.GetFileName、无任何清洗
  ```
- **问题**：SN 在登记时**无字符集校验**（`ZKDeviceManage.cshtml.cs:40-44` 只判非空 + 长度≤50；实体 `ZKDevice.cs:17` 只有 `[Required, MaxLength(50)]`），却能直接拼进文件名 → SN 含 `../` 可目录穿越写文件
- **要改成**：登记时校验字符集，**拒绝路径分隔符与 `..`**；落盘文件名加 `Path.GetFileName` + 清洗
- **验收**：登记 `../evil` 被拒；真机现有 SN 仍能正常登记与抓拍落盘
- **注意**：**施工前先查生产实际 SN 形态**（`SELECT SN FROM ZKDevice;`）。宁可用"拒绝 `/\..` + 长度上限"的黑名单式校验，也不要拍一个过窄的正则把在网设备拒掉

### A2 登记被拦时清理已落盘的身份证照片 `P1` `[亲验]`
- **位置**：`Pages/Employee/SelfRegister.cshtml.cs` `OnPostAsync`
- **当前代码**（`:63-64`）：
  ```csharp
  var photoUrl = await SaveIdCardPhotoAsync();     // 先落盘
  ... SubmitAsync(...)                             // 后校验（重复/黑名单在 EmployeeRegistrationService.cs:62-76）
  ```
  catch 分支（`:79-82`）只 `ErrorMessage = ex.Message`，**没有删除逻辑**
- **问题**：匿名可循环制造 ≤10MB 孤儿文件刷盘
- **要改成**：校验全部通过后再落盘；或失败路径删除已写文件
- **验收**：连续提交重复身份证 10 次，`PrivateUploads` 目录不产生新文件

### A3 `PrivateFilesController` 路径硬化 `P1` `[亲验]`
- **位置**：`Controllers/PrivateFilesController.cs:32-49`、`Helpers/PrivateFileStorage.cs:12`
- **当前代码**（`PrivateFilesController.cs:48-49`）：
  ```csharp
  var fullPath = Path.GetFullPath(Path.Combine(root, relativePath));
  if (!fullPath.StartsWith(root, StringComparison.OrdinalIgnoreCase)) return Forbid();   // 防路径穿越
  ```
  其中 `root` = `Path.Combine(env.ContentRootPath, "PrivateUploads")`（`PrivateFileStorage.cs:12`）
- **问题**：权限判据用 `segments`（`:32-33`）、读盘用另一路 `GetFullPath`（`:48`）—— 两者可被 `..` 拆开；且 `StartsWith(root)` **不带目录分隔符**，拦不住 root 内部跨目录
- **要改成**：比较 `root + Path.DirectorySeparatorChar` 前缀；显式拒绝含 `..` 的段
- **验收**：构造跨目录路径返回 403/404；正常四类文件读取不受影响
- **注**：**"可读 `appsettings.json`"的旧结论不成立**（已亲验：逃出 `PrivateUploads` 会被前缀拦）。但本条**不是纯纵深**——它直接削弱 V5 的归属校验（拿有权限的工号可读到 faces 目录下他人照片），**维持 P1**

### A4 `/iclock` 通道加固 `P1` `[亲验]`
- **位置**：`Controllers/ZKDeviceController.cs` `IsKnownDeviceAsync:32-34`、未知 SN 计数 `:43-61`、心跳/命令 `:172-208`
- **当前代码**：
  ```csharp
  // :32-34
  var known = await db.ZKDevices.AnyAsync(d => d.SN == sn && d.IsActive);
  // :77/:102/:167/:230/:246/:277 —— 六个端点对未知 SN 一律：
  return Content("UNKNOWN DEVICE", "text/plain", Gbk);
  ```
- **问题**（**与旧结论不同，注意**）：
  1. 未知 SN 的请求**已经被拒绝**，不存在"慢速枚举能拿到数据"；只有**计数器**（20 次/10 分钟）是"只记日志不拦截"
  2. **真正可利用的是**：心跳/命令接口（`:172-208`）对**任一已知 SN**（含他司设备）都明文返回该设备未确认命令的 `CommandText`，**完全没有设备归属校验**
- **要改成**：① 未知 SN 的计数升级为**按 IP/SN 的硬限流**；② 心跳/命令回执按**设备归属**校验（`ZKDevice.DepartmentId` 或 `UserZKDevice` 绑定）；③ "来源 IP 白名单"必须先拿到设备实际出口 IP 清单（见 §7-7），**没有清单不要擅自加，否则会打断生产设备**
- **验收**：模拟未知 SN 高频请求被限流；用他司 SN 拉命令队列被拒；真机心跳/上传不受影响

### A5 考勤组接口的角色守卫 —— ✅ **已修复（2026-09-21，`78e8bfa`），条目已删除**
### A6 跨司共用考勤组写权限 —— ✅ **本就不是缺陷（4 处早已是 `All`），条目已删除**

### A7 种子账号与初始口令 `P1` `[亲验]`
- **位置**：`Program.cs` `SeedAdminAsync:260-289`
- **当前代码**：
  ```csharp
  // :246 注释：播种默认管理员：不论什么环境，只要表已建好且库里还没有用户，就创建 admin / Admin@123
  // :263
  if (await db.Users.AnyAsync()) return;   // 已有用户就不播种
  // :288
  Log.Information("已创建默认管理员账号：admin / Admin@123");
  ```
  `:275-286` 建 User 时**未设** `MustChangePassword`
- **问题**：全环境播种、明文口令写进日志、且没有强制改密
- **要改成**：只在 Development 播种（或用显式开关）；**不再把明文密码写日志**；种子账号置 `MustChangePassword = true`
- **验收**：Production 环境清空 Users 表后重启**不会**重新出现 admin；日志中搜不到明文密码

### A8 改密链路 `P1` `[亲验]`
- **位置**：`Pages/Login.cshtml.cs`、`Pages/Account/ChangePassword.cshtml(.cs)`、`Controllers/AccountController.cs`、`Services/Implementations/UserService.cs`
- **当前事实**：`MustChangePassword` 的**代码引用共 5 处**（其余命中都是注释与迁移文件）：`UserService.cs:123`（建号时置 `true`）、`:192`（自己改密后置 `false`）、`:220`（管理员重置后置 `true`）、`Login.cshtml.cs:73`（登录后 `Redirect("/Account/ChangePassword?forced=1")`）、`AccountController.cs:51`（登录响应里回显）。**`Middlewares/` 下零引用**；限流策略只有 `Program.cs:218 LoginPolicy` 与 `:222 SelfRegisterPolicy`，**改密两处都没有 `[EnableRateLimiting]`**（实测 `EnableRateLimiting` 全仓 3 处：`AccountController.cs:24`、`Login.cshtml.cs:19`、`SelfRegister.cshtml.cs:19`）；`UserService.cs:191-194` 改密只改 hash + `MustChangePassword=false`，**无 SignOut、无 SecurityStamp**（实测全仓 `SecurityStamp` 零命中）
- **问题**：① `MustChangePassword` 只在登录后跳一次，直接访问其他页面即可绕过；② 改密无失败限流；③ 改密成功后既有会话不失效
- **要改成**：① 在中间件/授权层强制拦截（未改密的会话只能访问改密页与登出）；② 给改密加失败限流；③ 改密成功后重建身份（吊销旧会话）
- **验收**：用 `MustChangePassword` 账号直接访问 `/Report/MonthlyReport` 被弹回改密页；改密后旧 Cookie 失效

### A9 月份/年份参数校验 —— ✅ **已修复（2026-09-21，`78e8bfa`：报表/日历页非法年月参数不再 500），条目已删除**

### A10 管理页补 `ModelState.IsValid` `P2` `[亲验]`
- **位置**：`Pages/Admin/*.cshtml.cs` 中带 `OnPost*` 却无 `ModelState.IsValid` 的页面
- **实测**：该目录共 9 个文件，其中符合条件的正好 **7 个**：`DepartmentManage`、`GroupManage`、`HolidayManage`、`PunchAdjust`、`ShiftManage`、`UserManage`、`ZKDeviceManage`（`Dashboard`/`EmployeeInfo` 无 `OnPost`）；全目录 0 处 `ModelState.IsValid`
- **问题**：模型绑定失败直接取默认值（如空日期按 `0001-01-01` 入库）
- **要改成**：`OnPost*` 开头判 `ModelState.IsValid`，非法则回显错误
- **验收**：提交非法日期不再产生 `0001-01-01` 记录

### A11 自助登记二维码与部门绑定 `P2` `[亲验]`
- **位置**：生成端 `Pages/Admin/UserManage.cshtml.cs:67-68`（`RegistrationUrl`）+ `:158-166`（`OnGetQr`）；提交端 `Pages/Employee/SelfRegister.cshtml.cs:37`（`[BindProperty(SupportsGet = true)] int? DeptId`）+ `:75`
- **当前代码**：
  ```csharp
  // UserManage.cshtml.cs:67-68 —— 纯拼 query，无签名、无过期
  // SelfRegister.cshtml.cs:75
  DepartmentId = DeptId,
  ```
  `EmployeeRegistrationService.cs:57-58` 只判"部门存在"，查不到就置 null
- **问题**：`DepartmentId` 完全客户端可控 → 可把资料投到任意分公司待确认列表
- **要改成**：二维码带签名（含 deptId + 过期时间），提交时验签
- **验收**：手改 URL 里的 deptId 提交被拒；正常扫码流程不变
- **⚠️ 施工前必须确认**：现场是否已打印/张贴二维码？改签名会让**旧二维码全部失效**（见 §8-11）

---

## 3. B 批：考勤口径与数据正确性

> **第 9 轮整理**：B1~B6、B8~B14 已修复并**从本节删除**（见 §0.7/§10.1），本节只剩 **B7**（死字段，① 仍待处理）与 **B8**（范围判定未收敛）。
> 半天假跨午休口径与请假天数折算已按 §8-1 / §8-2 落实；**历史重算幂等（§8-8）与夜班上班卡跨午夜（§8-12）仍卡在待决策，不许擅自施工**。

### B1 补卡时间倒挂时静默跳过重算 —— ✅ **已修复（2026-09-21，`78e8bfa`：倒挂改为写入 `ApprovalNote` 提示人工核实），条目已删除**

### B2 月末汇总不随打卡刷新 —— ✅ **已修复（2026-09-21，`c89b5ad`），条目已删除**
### B3 "我的记录"与"我的日历"不一致（含 C3）—— ✅ **已修复（2026-09-21，`c89b5ad`），条目已删除**

### B4 审批与通知类缺陷（①②③）—— ✅ **已修复（2026-09-21，`c89b5ad`），条目已删除**
### B5 公告类缺陷（①②③）—— ✅ **已修复（2026-09-21，`c89b5ad`），条目已删除**
### B6 重试与错误处理 —— ✅ **已修复（2026-09-21，`c89b5ad`：`PunchAsync` 补日志、员工侧友好文案），条目已删除**

### B7 死字段（① 仍待处理；② 已修）`P2`
- **① `IsHoliday` / `LocationAbnormal` 有读无写 —— 2026-09-22 复核仍然成立**：实测 grep `IsHoliday = true` / `LocationAbnormal = true` 在业务代码中**零命中**；读点 `Dashboard.cshtml.cs:42-43`、`AttendanceService.cs:889`（`LocationAbnormalCount` 恒 0）。**二选一**：在打卡落库时写入，或在页面/导出里去掉这两处展示
- **验收**：看板不再显示恒 0 的"定位异常"，且 `IsHoliday` 字段有明确去向（写入或删除）
- **② PunchCard 死页面、B9 的 8 处 `escapeHtml` 副本** —— ✅ 已修（2026-09-21，`78e8bfa`），**B9 条目已删除**

### B8 重复校验规则收敛 —— 🟡 **部分完成（2026-09-21，`c89b5ad` + `78e8bfa`），剩下范围判定** `P3`
- **已完成**：手机号/身份证号格式校验已抽成 `Helpers/ContactValidationHelper.cs`，`UserManage` 页面与 `AdminController` 接口共用一份；设备范围两侧口径也已统一（随 §8-5 一起放开为"不查部门范围"）
- **仍未收敛（本条剩余部分）**：**管理范围判定**仍是两份代码 —— `UserManage.cshtml.cs` 的 `ValidateScopeForSaveAsync`（对非总部超管直接报错）vs `Controllers/AdminController.cs:205-223` 的 `ValidateUserScopeAsync`（不报错、强制钳到操作者自己范围）；两者**行为不同**，收敛时必须先定保留哪一种语义
- **要改成**：把范围判定收敛到服务层；**不要顺手把两种语义改一致**（先定语义再收敛）
- **验收**：同一非法输入，页面与接口的**提示与结果**一致且符合最终确定的语义

### B10 二级审批的两个节点可能指向同一个人 —— ✅ **已修复（2026-09-21，`78e8bfa`：二级与一级相同就不生成二级节点），条目已删除**
### B11 旷工标记没有补跑机制 —— ✅ **已修复（2026-09-21，`78e8bfa`：补上次日凌晨补跑机制），条目已删除**

### B12 补跑会重复发"下班未打卡提醒" —— ✅ **已修复（2026-09-21，`166ae18`），条目已删除**
### B13 半天假提交校验漏掉窗口边界点 —— ✅ **已修复（2026-09-21，`166ae18`），条目已删除**
### B14 同日请假区间"跨越"午间窗口仍会多算 1 小时 —— ✅ **已修复（2026-09-22，`37bfafe`：补上"整段跨过窗口"的情况），条目已删除**

---

## 4. C 批：性能

### C1 设备同步逐行查库 `P2` `[亲验]`
- **位置 / 当前代码**：
  ```csharp
  // ZKDeviceSyncService.cs:99  批量预载 holidays（仅结算用）
  // :125  foreach (打卡) { ... }
  // :155  var isRestDay = await AttendanceService.IsNonCompRestDayAsync(db, workDate, shift, punchGroupId);
  // :234  foreach (touchedKeys) { ... :282 又一次 IsNonCompRestDayAsync ... :265-270 查当天打卡 }
  ```
  `IsNonCompRestDayAsync` 内部执行 `db.Holidays.AnyAsync(...)`（`AttendanceService.cs:966-973`）
- **问题**：打卡循环里**每条打卡 1 次查询**，预载的假期**没有复用**；结算阶段每 (人,日) 再查一次（其中"查当天打卡"仅当该班次配了午间必打卡窗口才发生）。断网补传 500 条的 SQL 次数 **≥500，最坏约 1500**
- **要改成**：打卡循环复用已预载假期；(人,日) 维度批量取当天打卡
- **验收**：补传 500 条时 SQL 次数从 ≥500 降到个位数（用 EF 日志计数）

### C2 循环内查库（N+1）`P2` `[亲验]`
- **⚠️ 旧结论"月度汇总 2082×30 ≈ 6.2 万次同步往返"不成立，不要照它改**：
  ```csharp
  // AttendanceService.cs:698  var recordsByUser = (await db.AttendanceRecords ...   ← 已批量预载
  // :717  var groupsById = await db.AttendanceGroups.Where(...).ToDictionaryAsync(...)
  // :747  var group = user.AttendanceGroupId.HasValue ? groupsById.GetValueOrDefault(...) : null;
  ```
  汇总循环内取字典，**从不调用 `CalcWorkHours`**（HEAD 版本同样如此）
- **真正的循环内查库点（实测）**：
  1. **老数据补算分支** `GenerateMonthlySummaryAsync:758-766`：每条需补算的记录调一次 `IsNonCompRestDayAsync`（条件性）
  2. **请假审批回写** `AttendanceService.cs:1097-1164`：对区间内**每一天**调 `RecalcWorkHoursAfterManualPunchAsync`，该链路每天查库约 6 次（`:1265`/`:1266`/`:1290`/`:1348`/`:1362`/`:1384`）→ **一张跨 3 天假单 ≈ 18 次查询**
  3. `CalcWorkHours:1346-1350` 用**同步** `db.AttendanceGroups.Find`（`:1348`）→ 阻塞线程；调用频率低，属**轻微**问题，不要夸大
- **要改成**：① 补算分支复用已预载假期；② 回写链路按天批量预载；③（可选）`CalcWorkHours` 改异步/接收已加载 group
- **验收**：给出"改前/改后，回写一张跨 3 天假单"的 SQL 次数与耗时；**禁止改动 `GenerateMonthlySummaryAsync` 已有的批量预载**

### C3 日历页每次打开都写库 —— ✅ **已修复（2026-09-21，`c89b5ad`：随 B3 用 `EnsureMonthlySummaryFreshAsync` 统一，无变化不再写库），条目已删除**

### C4 看板/统计全量入内存 `P2` `[亲验]`
- **位置**：`AttendanceService.BuildUserIdQueryAsync`（定义 `:1710-1719`，返回 `List<int>`；原文档写的 `:361` 是**调用点**）、下钻 `GetTodayStatsDetailAsync:874-938`
- **问题**：先 `ToListAsync` 全部 userId（`:1718`），调用侧一律 `userIds.Contains(...)`（`:365`/`:432`/`:826`/`:884`）→ 超大 `IN`；下钻 `:879-885` 载入范围内全部用户 + 当日全部记录，`:908-937` **无分页**
- **要改成**：改子查询/`JOIN` 让 SQL 侧过滤；下钻分页
- **验收**：几千人范围下无超大 `IN`，SQL 走索引

### C5 导出全量载入内存 `P3` `[亲验]`
- **位置 / 当前代码**：
  ```csharp
  // EmployeeInfo.cshtml.cs:72
  var (users, _) = await userService.GetUsersAsync(deptId: deptId, keyword: keyword, pageIndex: 1, pageSize: 100_000);
  // ExcelExportHelper.cs:656-661（ToBytes）
  using var ms = new MemoryStream(); wb.Write(ms, leaveOpen: true); return ms.ToArray();
  ```
- **问题**：峰值 ≈2× 文件；用的是 `XSSFWorkbook`（**整本工作簿常驻内存**），实际更高。**这个 2× 对全部 6 个导出都成立**（`ExportMonthlySummary`/`ExportDailyStatusReport`/`ExportTemplateReport`/`ExportShiftAssignments`/`ExportClockTimeSheet`/`ExportEmployeeList`，调用点 `:101/:167/:280/:363/:437/:495`）
- **要改成**：分批取数、直接写响应流 —— **改 `ToBytes` 一处即覆盖全部 6 个导出**
- **验收**：366 天导出内存峰值明显下降；导出文件内容不变

### C6 补索引（需迁移）`P2` `[亲验]`
- **要加**：`Notification(UserId, IsRead)`、`Announcement(ScopeId)`、`AttendanceGroup.ClerkUserId`；`AttendancePunch` 的 `(UserId, PunchTime)` 查询目前只能用唯一索引 `(UserId, PunchType, PunchTime)`（`AttendanceDbContext.cs:139`）的前缀，评估是否需补
- **实测三条索引都不存在**：`Notification`（`:241-247` 只有外键配置）、`Announcement`（`:250-256` 无 `HasIndex`；`Announcement.cs:31` 的 `ScopeId` 是标量无导航）、`AttendanceGroup`（只有 `:342` 种子数据；`AttendanceGroup.cs:28` 的 `ClerkUserId` 同为标量）
- **⚠️ 先决定"扩展还是替换"**：`Notification` **已有** `IX_Notification_UserId`（`Migrations/20260618023257_InitialCreate.cs:529` 的外键约定索引），再加 `(UserId, IsRead)` 会出现前缀重复
- **必须同步**：迁移 + `AttendanceDbContextModelSnapshot.cs` + `migrate.sql`
- **验收**：`EXPLAIN` 显示用上索引；通知轮询（每 60 秒）与看板查询变快

### C7 `OnValidatePrincipal` 与中间件重复查库 `P3` `[亲验]`
- **当前代码**：
  ```csharp
  // Program.cs:117（OnValidatePrincipal，:107 挂载）—— 取整行
  var user = await db.Users.AsNoTracking().FirstOrDefaultAsync(u => u.Id == userId);
  // Middlewares/CurrentUserMiddleware.cs:56 —— 同一 userId 再查一次（6 字段投影）
  .Where(u => u.Id == userId)
  ```
- **问题**：同一请求查两次同一行，2000 人量级约 +0.5 QPS，**可忽略**
- **可选改法**：把中间件挪到 `UseAuthorization` 之前合并成一次查询（管线顺序已核实：`Program.cs:355` 在 `:356` 之前，改法可行）
- **⚠️ 合并注意**：两处取的行**不一样**（整行 vs 6 字段投影），合并后要补齐中间件需要的字段，否则会静默丢掉角色/范围的实时性
- **验收**：功能不变；两处取值一致

---

## 5. D 批：功能改动

### D1 "文员"也算正式工 —— ✅ **已完成（2026-09-21，`c89b5ad`：列表/员工信息页/导出 Excel 三处统一显示"正式工"），条目已删除**

---

## 6. E 批：测试

**现状（2026-09-22 更新）**：`AttendanceSystem.Tests` 现有 **6 个测试文件**（新增 `ApprovalStepGenerationTests.cs`，覆盖 B10"二级审批重复节点"回归），`dotnet test` 实测 **54 通过 / 0 失败**，框架 **xUnit 2.9.3**（+ `Microsoft.NET.Test.Sdk 17.14.1`、`Microsoft.EntityFrameworkCore.Sqlite 9.0.9`，用 `DataSource=:memory:`）：
`ApprovalServiceConcurrencyTests`（8）、`MidCheckWindowLogicTests`（15）、`LeaveHoursTests`（17）、`ExcelExportColumnAlignmentTests`（1），外加辅助类 `FakeAttendanceService.cs`（除 `UpdateAttendanceAfterApprovalAsync` 外**全部 `throw new NotImplementedException()`** → 这就是"端到端打卡测不了"的直接原因）。

| 编号 | 缺口 | 优先级 | 说明 |
|---|---|---|---|
| **E1** | ① 审批事务/竞态（**已覆盖，不要重写**）② 重试策略（**未覆盖**） | `P1` | ① 现有 8 个测试已覆盖事务、`ExecuteUpdateAsync` 抢占、撤销竞态、两级递进；② SQLite 未启用重试策略 → 测不出 09-18 那个 P0。**只需补 ②**：真实 MySQL / Testcontainers，验证"配了 `EnableRetryOnFailure` 时事务仍能提交" |
| **E2** | `PunchCoreAsync` 端到端 | `P1` | 全系统唯一打卡入口，零端到端测试（`FakeAttendanceService.cs:23+` 全 throw）。覆盖：跨天续接、同人同类型同分钟去重、`DbUpdateException` 重试、状态白名单 |
| **E3** | 远程打卡"当天未打的下班卡不受次数上限影响" | `P1` | 逻辑在 `RemotePunch.cshtml.cs:102-103`（`isComplementaryClockOut`），`:103-115` 跳过间隔闸门、`:122-139` 跳过两道次数闸门；测试无任何引用 |
| **E4** | `OnValidatePrincipal`（角色变更/停用即时生效） | `P2` | 09-21 新增逻辑，零测试。**✅ 状态更新（第 9 轮）**：被测代码**已提交**（`7404015` 起，现 HEAD `37bfafe`），"测试必须与代码一起提交"的顾虑已消失 → 直接补测试即可 |
| **E5** | 请假跨午休口径 | `P2` | **等 §8-1 决策后**再写（现有 `LeaveHoursTests:49-59` 的算例只有 4 小时，走不到扣午休分支） |
| **E6** | `MarkAbsentAsync` 新口径 | `P2` | 建议先抽成纯函数再测（会批量产出旷工记录，改动前必须锁定行为） |
| **E7** | `ImageValidationHelper` | `P2` | 4 种魔数 + 伪装文件。**✅ 状态更新（第 9 轮）**：该文件**已提交**，不再有"新文件未跟踪"的丢失风险 → 直接补测试即可 |
| **E8** | 权限回归（安全回归） | `P1` | 目前零权限测试（全工程 grep `DeptScope`/`Scope` 零命中）。覆盖：无范围 Clerk 不得跨司、**A5 修完后的"无范围 Clerk 不得建考勤组"**、公告 `All` 口径、考勤组/设备范围校验、**§8-14 的"无范围 Clerk 不得触发全库重算"** |
| **E9** | `ComputeWorkHours` 边界 | `P3` | **已有覆盖（不要重写）**：`MidCheckWindowLogicTests:136-147` 覆盖 6h/9h 阈值（`8:30→17:30` 正是**恰好 9 小时**边界）、`:161-167` 倒挂、`:149-159` 取整幂等。**未覆盖**：**恰好 6 小时**、跨天 24h+、`lunch/dinner` 非默认值 |
| **E10** | 索引迁移的 `Down()` | `P3` | 现有测试用 `EnsureCreated()`（`ApprovalServiceConcurrencyTests.cs:38`），**测试工程里没有任何迁移调用**，所以测试中 `Up`/`Down` 从没被执行过。**注意别混淆**：`Program.cs:237` 在**开发环境**启动时确实会 `await dbCtx.Database.MigrateAsync()`（`:230` 注释说明生产不走这条、生产靠手工 `migrate.sql`）→ 也就是说**开发库跑过 `Up`、但从没跑过 `Down`**。需另写跑迁移的测试，或人工在测试库执行 `Down()` |

---

## 7. ❓ 只能在生产/真机上确认（施工者做不了）

1. `ZKDeviceController.cs:172/199` 的 `SentAt == claimedAt` 在 `datetime(6)` 下是否因舍入导致设备命令永不确认（静态层面读写同一个参数，看不出问题）
2. 阿里云 SDK `ex.Message` 是否含临时 OSS URL；重试/取消的请求是否计费
3. 前置 nginx 是否规范化 `%2e%2e`
4. 生产 `__EFMigrationsHistory` 与 `migrate.sql` 是否一致；**必须确认里面有 `20260918075907_ChangeActualWorkdaysToDecimal`**（若没有：`ActualWorkdays` 仍是 `int`，出勤天数 21.5 会被截断成 21/22，**直接影响工资**）
5. Capacitor 安卓壳真机表现；壳是否仍在使用
6. 审批"驳回级联作废"与下一级同时提交的覆盖窗口（需压测）
7. 设备固件是否支持签名/来源校验；**设备实际出口 IP 清单**（做 A4 的来源校验前必须有）
8. 生产库数据事实：是否已存在"无范围管理类账号"、`LeaveHours<=0` 分布、重复打卡/未闭合夜班量
9. 有多少在职员工排的班次**没配午间窗口**（决定 §8-3）

**只读核查 SQL**（人工跑）：

> 表名已核对：库里是**单数 PascalCase**（`User` / `AttendanceRecord` / `ShiftSchedule` / `ShiftAssignment`），不是复数。班次挂在 `ShiftSchedule`（含 `MidCheckWindows`），员工↔班次是**按日排班**在 `ShiftAssignment(UserId, ShiftScheduleId, WorkDate)`，`User` 表上**没有** ShiftId 字段。

```sql
-- 1) 请假工时异常巡检（截至 09-21 生产为 0 条）
SELECT COUNT(*), MIN(WorkDate), MAX(WorkDate)
FROM AttendanceRecord WHERE AttendanceStatus = 6 AND LeaveHours <= 0;

-- 2) 是否已存在"无范围的管理类账号"（Role: Admin=1, Clerk=2）
SELECT COUNT(*) FROM User WHERE Role IN (1,2) AND ScopedDepartmentId IS NULL;

-- 3) 没配午间窗口的班次，近 30 天有多少在职员工排到（决定 §8-3）
SELECT ss.Id, ss.ShiftName, COUNT(DISTINCT sa.UserId) AS 涉及在职员工数
FROM ShiftSchedule ss
JOIN ShiftAssignment sa ON sa.ShiftScheduleId = ss.Id
JOIN User u          ON u.Id = sa.UserId AND u.IsActive = 1
WHERE (ss.MidCheckWindows IS NULL OR ss.MidCheckWindows = '')
  AND sa.WorkDate >= DATE_SUB(CURDATE(), INTERVAL 30 DAY)
GROUP BY ss.Id, ss.ShiftName;

-- 4) 迁移历史与列类型（判断工资口径是否已生效）
SELECT MigrationId FROM __EFMigrationsHistory ORDER BY MigrationId DESC LIMIT 8;
SHOW COLUMNS FROM MonthlyAttendanceSummary LIKE 'ActualWorkdays';   -- 应为 decimal(65,30)
```

---

## 8. 👤 待用户决策（**禁止擅自施工**）

| # | 决策项 | 说明与备选 |
|---|---|---|
| ~~1~~ | ✅ **已落实（备选 B，2026-09-21，`78e8bfa`）**：提交页已限制请假起止时间不能落在班次午间窗口内。**遗留**：备选 A（按付薪时段求交）与"没配午间窗口的班次"仍见 §8-3 → 本条主体已从待决策表删除 |
| ~~2~~ | ✅ **已落实（2026-09-21，`78e8bfa`）**：`ResolveLeaveDaysFraction` 已改为按比例就近取整到 0.5 天（`≥0.75 → 1 天`、`≥0.25 → 0.5 天`、否则 0）→ 本条已从待决策表删除 |
| **3** | 没配午间窗口的班次要不要补兜底 | 新的半天假口径依赖"扣窗口"，这类班次会退回阈值口径。需先看 §7-9 的统计 |
| **4** | 设备隔离取消后的配套加固 | 建议三条：① 设备列表**不回显 SN**（勾选提交的是 `Id`）；② 设备标签补**所属分公司**（否则误勾后**无任何提示**就把员工工号/姓名推给对方设备）；③ 若必须看 SN，先给 `/iclock` 补来源凭证（A4） |
| ~~5~~ | ✅ **已落实（2026-09-21，`78e8bfa`）**：接口侧设备范围校验已与网页一致放开，并保留了"设备存在且启用"检查 → **本条已从待决策表删除** |
| **6** | 登记全局查重是否刻意"集团防重复用工" | 若是有意规则则接受，只需加限频；否则按范围查重或加验证码 |
| **7** | 黑名单共享边界 | 全公司共享的是"名单"，还是含完整证件资料/照片？（现状把身份证号/住址/紧急联系人序列化进弹窗 DOM） |
| **8** | **历史月份重算后数字会变** | 补算读**当前**考勤组扣时（`AttendanceService.cs:748-749` 的 `LunchBreakMinutes`/`DinnerBreakMinutes`）与**当前** `Holiday` 表 → 改配置后重开报表，历史月份数字会变（出勤天数规则刚改过，08~09 月重算会从 22 变 21.5 之类）。**要不要**引入"按当日配置快照"？**处理前必须先备份 `MonthlyAttendanceSummary`** |
| ~~9~~ | ✅ **已作废**：该接口早已删除（`Controllers/AttendanceController.cs:15-19`）→ **本条已从待决策表删除** |
| **10** | 导出脱敏 / 独立审计表 / 假期额度 / 加班上限 | 此前均记为暂缓，需要时再排期 |
| **11** | 现场是否已张贴自助登记二维码 | 决定 A11 能不能改签名（旧码会失效） |
| **12** | 夜班"上班卡"跨午夜归属 | `AttendanceService.cs:67` `if (request.PunchType is PunchType.ClockOut or PunchType.MidCheck)` —— **`ClockIn` 不在其中** → 23:00 上班者首卡落 00:05 会被记到新的一天且判"正常"，昨天永远停在"未打卡"。修法需先定归属规则（按班次起始时间反推？还是允许 `ClockIn` 也续接昨天？） |
| ~~13~~ | ✅ **已解决（2026-09-22 复核）**：`git status` 显示工作区只剩本文档一处改动，**代码改动已全部提交**（最新 `37bfafe`）→ 本条已从待决策表删除。历史记录：`| **13** | **工作区全部改动未提交（代码相关 24 个）** —— **最紧急** | 实测：**① untracked、Git 里根本不存在（删了永久丢失）3 个**：`Helpers/ImageValidationHelper.cs`、`Migrations/20260921010735_AddApprovalStepApproverStatusIndex.cs`、`…Designer.cs`；**② 已跟踪未提交 19 个**（`AttendanceService.cs` 165 行、`Program.cs` +39、`AttendanceBackgroundService.cs` +22、`AttendanceDbContext.cs`、`ExcelExportHelper.cs`、`AttendanceDbContextModelSnapshot.cs`、`AttendanceRecord.cs`、`AttendanceDTOs.cs`、`UserManage.cshtml(.cs)`、`SelfRegister.cshtml.cs`、`MyCalendar.cshtml`、`Pages/Attendance/MyRecord.cshtml`、`Pages/Report/MonthlyReport.cshtml`、`Pages/Approval/ApplySubmit.cshtml`、`migrate.sql` +43、3 个测试文件）；**③ 已删除 2 个**（`FaceTest` 页）。生产在跑其中一部分而仓库里没有 → 回滚只能回到 `9784347`，丢掉 09-18 之后全部修复。**⚠️ 提交陷阱**：`ModelSnapshot.cs` 已改（含新索引）但迁移文件是 untracked —— `git commit -a` 会**提交 snapshot、漏掉迁移文件**，此后 `dotnet ef migrations script` 生成的 `migrate.sql` 会缺索引。**必须显式 `git add` 那 3 个文件** | |
| ~~14~~ | ✅ **已修复（2026-09-21，`78e8bfa`）**：全库重算入口已按"必须是 Admin 且不受限"收敛口径 → 本条已从待决策表删除 |
| ~~15~~ | ✅ **已修复（2026-09-22，`37bfafe`）**：公告可见范围已下沉为服务方法参数 → 本条已从待决策表删除 |
| ~~16~~ | ✅ **已修复（2026-09-21，`78e8bfa`）**：受限管理员编辑设备不再改写归属部门 → 本条已从待决策表删除 |
| ~~17~~ | ✅ **已修复（2026-09-21，`78e8bfa`）**：停用中的考勤机保留员工绑定，不再被静默解绑与误发删除命令 → 本条已从待决策表删除 |

---

## 9. ⏸ 暂缓（**禁止施工**）

- `appsettings.json` 明文密钥：**已确认未被 git 跟踪**（`.gitignore:12-13`，`appsettings.Example.json` 例外），风险仅在被复制进 `bin/publish-*` 与部署包 → 部署流程已要求删除，不改代码
- `MidCheckWindows` 去重逻辑在两个解析方法里各一份：各有调用场景，**正确性已用单测锁定，本轮不动**
- 假期额度、加班上限、独立审计表、导出脱敏（见 §8-10）
- 数据模型层大改：无并发令牌（`RowVersion`）、无 `CheckConstraint`、无 `HasPrecision`（decimal 全落 `decimal(65,30)`）、时间列无 UTC 归一 → **架构级改造，未批准前不要动**
- **删用户会级联删除其全部打卡/考勤记录/月度汇总/通知/人脸尝试**（`AttendanceRecord`/`AttendancePunch`/`Notification`/`FaceVerifyAttempt` 显式 `Cascade`；`MonthlyAttendanceSummary` 无显式配置、级联来自 EF 约定，迁移里确实是 `ON DELETE CASCADE`）。**指向 User 的 `Restrict` 共 3 处**（`ApprovalStep.ApproverUserId`、`AttendanceGroupApprover.UserId`、`Announcement.PublisherUserId`；全库 `RESTRICT` 共 4 处）。
  **但实际门槛比"级联"听起来高得多**：`UserService.DeleteUserAsync:327-366` 在真正删除前有 **4 道拦截**，任一命中就抛中文提示拒绝删除 —— ① 他是某个考勤组的审批人（`:333-339`）；② 有审批记录（`:344-345`，"只能停用"）；③ 发布过公告（`:346-347`）；④ 有人认他当直属上级（`:352-355`，因为 `SupervisorUserId` 是 SetNull，直接删会静默断掉下属的审批链）。**顺序也处理正确**：`:359-361` 先排好设备解绑命令再删 User（否则 `UserZKDevice` 级联后查不到该往哪台设备发删除）→ 要不要改"软删"是产品决策，**不要自作主张改级联，也不要删掉那 4 道拦截**
- **33 个**历史迁移（排除 `*.Designer.cs` 与快照后的计数）**全部无 `IF NOT EXISTS`**（实测 0/33；其中 `20260707011708` 先 `DROP TABLE`、`20260821022210` 先静默 DELETE 真实打卡数据再换唯一索引）→ **历史迁移文件不要回改**，只保证新迁移可重复执行

---

## 10. 附录

### 10.1 已修复项（**不要重复修**）

提权链（分公司账号可造"无范围账号"→ 全公司权限，页面 + 接口两条路已钳制）、6 处存储型 XSS（铃铛、公告已读名单、审批详情、补卡备注、我的日历备注、文件名/地图文本）、公告 `Any/All` 口径、排班导出跨司、审批"通过/驳回全失败"（`EnableRetryOnFailure` 与手动事务冲突）、降权实时性（`OnValidatePrincipal`）、设备推送只认已绑定员工、敏感文件归属校验、身份证照片魔数校验（`ImageValidationHelper`）、FaceTest 诊断页删除、登录接口限流、改密长度校验下沉服务层。

**第 2/3 轮复验确认"已修、不要重复修"**（逐条见 §1.1）：待确认登记认领/驳回跨司校验、审批兜底人范围、公告撤下/已读范围、敏感文件四类归属校验、共享考勤组名册与导出范围、建人改人 `AttendanceGroupId` 校验、铃铛转义、**跨司共用考勤组写权限（A6，4 处已是 `All`）**、四类申请重复提交拦截。

**判定为"合理设计、非缺陷"**：`ApproverCoversApplicantAsync` 对不受限账号恒真（V10）、加班回写按天累加（V11，已被"节点原子认领"防重复）。

**已作废**：`POST /api/Attendance/punch` 接口本身已被删除（§8-9）。

**口径类已修**：请假时长公式统一（`ComputeLeaveHoursForDay`）、半天假支持、半天假"没打下班卡"估算封顶、`HasLeaveOverlapForDay`、取整统一半小时（`FloorToHalf`）、出勤天数按比例（`ResolveAttendanceDayCredit` + `ActualWorkdays` 改 decimal）、模板表口径 + "请假天数"列、下班时间"取更晚"、旷工判断改用 `IsShiftWeeklyRestDay`、`LeaveHours` 由 `=` 改 `+=`、`ApprovalNote` 改追加 + 截断、午间窗口重复配置去重。

**存量数据处置**：`LeaveHours` 回填 15 条（09-18）、半天假没打下班卡回填 5 条（09-21）、重复加班扣除 72 条 445 小时（均改前备份）。

**第 9 轮整理：以下条目已确认修复/完成/作废，并已从 §1~§6、§8、§11 的表格中删除（保留此处以便追溯，不要再开工单）**

| 已删编号 | 状态 | 依据 |
|---|---|---|
| V1~V7、V9、V12、V8① | ✅ 已修（前几轮复验确认） | §1.1 原表 + `7404015`/`9784347` |
| V10、V11 | ⏭️ 合理设计，非缺陷 | §1.2 原表 |
| **A5** 考勤组接口角色守卫 | ✅ 已修 | `78e8bfa` |
| **A6** 跨司考勤组写权限 | ⏭️ 本就不是缺陷（4 处早已是 `All`） | 前几轮亲验 |
| **A9** 月份/年份参数校验 | ✅ 已修（报表/日历页不再 500） | `78e8bfa` |
| **B1** 倒挂静默跳过 | ✅ 已修（改为写 `ApprovalNote` 待人工核实） | `78e8bfa` |
| **B2** 月末汇总不随打卡刷新 | ✅ 已修（打卡/设备同步落库后增量刷新） | `c89b5ad` |
| **B3 + C3** 两页汇总不一致 / 日历页每次写库 | ✅ 已修（抽出 `EnsureMonthlySummaryFreshAsync`） | `c89b5ad` |
| **B4** 审批通知①②③ | ✅ 已修 | `c89b5ad` |
| **B5** 公告①②③ | ✅ 已修 | `c89b5ad` |
| **B6** 打卡无日志/异常直显 | ✅ 已修 | `c89b5ad` |
| **B7②** PunchCard 死页面 | ✅ 已修 | `78e8bfa` |
| **B9** `escapeHtml` 8 处副本 | ✅ 已修 | `78e8bfa` |
| **B10** 二级审批同人 | ✅ 已修 | `78e8bfa` |
| **B11** 旷工标记无补跑 | ✅ 已修（补上次日凌晨补跑） | `78e8bfa` |
| **B12** 补跑重复发提醒 | ✅ 已修 | `166ae18` |
| **B13** 半天假窗口边界点 | ✅ 已修 | `166ae18` |
| **B14** 请假跨窗口多算 1h | ✅ 已修 | `37bfafe` |
| **D1** 文员计入正式工 | ✅ 已完成 | `c89b5ad` |
| §8-1 / §8-2 / §8-5 / §8-9 / §8-13 / §8-14 / §8-15 / §8-16 / §8-17 | ✅ 已落实/已作废（§8 保留 8 项待决策） | `78e8bfa`、`37bfafe`、本轮 `git status` 复核 |

**仍未完成、保留在表里的 B 批项**：**B7①**（`IsHoliday`/`LocationAbnormal` 有读无写，2026-09-22 复核仍成立）、**B8**（手机号/身份证校验已收敛，管理范围判定仍是两份代码）。

### 10.2 ⚠️ 被推翻的错误结论（**照这些改会改坏**）

| 错误结论 | 事实 | 依据 |
|---|---|---|
| "路径穿越可读 `appsettings.json`（含 DB 口令）" | **不成立**：`GetRoot` = `ContentRoot/PrivateUploads`，逃出即被前缀拦 | `[亲验]` |
| "被成本闸门拦截的尝试没有写 `FaceVerifyAttempt`" | **误读需求**：确实写了一条带 `BlockedReason` 的行、查询处显式排除，是"记录拦截事件供看板统计"的有意设计 | `[亲验]` |
| "月末汇总 2/3 号补跑守卫恒 false、补跑失效" | **误判**：1 号成功后本不需补跑；真问题是"打卡不刷新汇总"（= B2） | `[精读]` |
| "审批人多级 `ApprovalLevel` 在代码中不存在" | **部分错**：考勤组实体确有 `ApprovalLevel`；不存在的是"审批人节点表上的层级列" | `[亲验]` |
| "同一函数行号前后不一致说明代码被改错" | 是审查者读到了**正在编辑的工作区**（未提交改动把中段行号推后 7~14 行） | `[亲验]` |
| **"跨司共用考勤组写权限实现是 `Any`，4 处复制同款"** | **错**：4 处 `IsGroupWritableAsync` 早已是 `All`（`:186`/`:150`/`:397`/`:353`）。那个 `Any` 在**只读**的 `IsGroupInScopeAsync` 里（`Count == 0 → true` = 零部门组可见）。**照原结论改会破坏"零部门组可见"** | `[亲验]` |
| **"考勤组接口 `POST /api/admin/attendance-groups` 零范围校验"** | **错**：真实路由 `POST /api/Admin/attendance-groups`，且有 `if (Cu.IsScoped) return Forbid();`。真问题是**该守卫不判角色** | `[亲验]` |
| **"月度汇总 2082×30 ≈ 6.2 万次同步往返"** | **错**：汇总早已批量预载（`:698`/`:717`），**从不调 `CalcWorkHours`**。真实 N+1 在补算分支与请假回写链路 | `[亲验]` |
| **"打卡重试失败后抛通用文案、看不到真因"** | **半错**：那句通用文案是**不可达死代码**；真问题是 `PunchAsync` **完全没有日志** + 把 EF 原始异常显示给员工 | `[亲验]` |
| **"`/api/Attendance/punch` 接口真实存在、需要决策"** | **错**：接口**已被删除**（`AttendanceController.cs:15-19`） | `[亲验]` |
| **"未知 SN 只计数不拦截，可慢速枚举"** | **半错**：未知 SN 的请求已被 6 个 `/iclock` 端点**直接拒绝**（`return Content("UNKNOWN DEVICE", ...)`），只有**计数器**是"只记日志" | `[亲验]` |
| **"日历页读全公司假期表 + 全量考勤组"** | **错**：只重算本人当月、只取当月假期（限公司级/本人组）、只查该员工的考勤组。"每次打开都写库"这半是对的 | `[亲验]` |
| **"`UserService.ValidateUserScopeAsync`"** | **错**：定义在 `Controllers/AdminController.cs:205`，`UserService` 里没有 | `[复核]` |
| **"`IsAnnouncementInScopeAsync` 定义在 `Publish.cshtml.cs:101`"** | **错**：定义在 `Services/Implementations/AnnouncementService.cs:86`；`Publish.cshtml.cs:101` 只是一句提到它的注释 | `[亲验]` |
| **"`IsGroupWritableAsync` 共 4 份副本，改一处必须四处同改"** | **不完整**：它确为 4 份；但相邻的 `IsGroupInScopeAsync` 有 **5 份**（多一个 `UserManage.cshtml.cs:444`），两套都要一起看 | `[复核]` |
| **"半天假填 13:00–17:30 得 3.5h"** | **表述有歧义（数字没错）**：3.5h 是"封顶后的**可结算工时上限**"，对应**请假时长 4.5h**。已在 §8-1 写清 | `[亲验]` |

### 10.3 阅读覆盖（结论可靠度边界）

- **逐行精读**：`AttendanceService.cs`、`UserManage.cshtml.cs`、`ApprovalService.cs`、`ZKDeviceController.cs`、`ZKDeviceSyncService.cs`
- **亲自核对**：`Program.cs`、`PrivateFilesController.cs`、`PrivateFileStorage.cs`、`AccountController.cs`、`NotificationController.cs`、`CurrentUserMiddleware.cs`、`AnnouncementService.cs`、`ExcelExportHelper.cs`、`AttendanceDbContext.cs`、`User.cs`、`ShiftSchedule.cs`、`AttendanceGroup.cs`、`AdminController.cs`（设备/考勤组部分）、`GroupManage.cshtml.cs`、`ShiftManage.cshtml.cs`、`MyCalendar.cshtml.cs`、`MyRecord.cshtml.cs`、`MonthlyReport.cshtml.cs`、`Publish.cshtml.cs`、`_Layout.cshtml`、`UserManage.cshtml`、`EmployeeInfo.cshtml`、`AttendanceController.cs`、`ReportController.cs`、`SelfRegister.cshtml.cs`、`ZKDeviceManage.cshtml.cs`、`Login.cshtml.cs`、`ChangePassword.cshtml.cs`、`PunchCard.cshtml.cs`、`UserService.cs`、`AttendanceSystem.Tests/*`、`.gitignore`、`migrate.sql`、`Migrations/*`
- **未逐行复核（仅并行审查覆盖）**：`UserService.cs`、`AliyunFaceClient.cs`、`ExcelExportHelper.cs` 大部分、多数 `.cshtml`、`Pages/Admin/*` 其余页面、`AttendanceBackgroundService.cs`（部分）、`mobile-app/`、`wwwroot/`
- **本文档未运行、未构建、未连接数据库**；§7 的 9 项需人工在生产/真机确认

---

### 10.4 第 7 轮全面审查新增问题（B15 起，**待你决定是否纳入施工**）

> **来源**：2026-09-21 第 7 轮全面审查（5 路并行子代理 + 我逐条复核高严重度项）。**已排除** A1~A11、B1~B14、C1~C7、E1~E10、§7~§9、§10.1 全部已报/已修项。
> **核实标记**：`[亲验]` = 我打开当前代码逐行确认过；`[引文]` = 子代理给出「文件:行 + 原文代码」，我未逐行复核（按规则 1 施工前仍需核对引文）。
> **状态**：这些条目**尚未**并入 §2~§6 批次、**尚未**进入 §11 回执表。你确认后我按批次归位并补回执行。

#### 🔴 高（8 条）

| 编号 | 类别 | 位置 | 问题 | 建议修法 | 核实 |
|---|---|---|---|---|---|
| **B15** | 安全/越权 | `Services/Implementations/DeptScopeService.cs:60-61` + `Controllers/AdminController.cs:19,147-149` | "不受限"只判 `ScopedDepartmentId == null`、**不看角色**，而 `ManagePolicy` 含 Clerk → **无范围文员 = 全公司管理员**：可重置任意人（含 `admin`）密码且响应体带回**新明文**；这种账号新建的账号也拿到 null 范围（自我复制） | "不受限"改为 `Role == Admin && !IsScoped`；重置密码加"不能动权限高于自己的账号"；明文改一次性展示 | `[亲验]` |
| **B16** | 安全（存储型 XSS） | `Pages/Approval/PendingApproval.cshtml:388-389` + `Models/DTOs/ApprovalDTOs.cs:35` | 附件 URL `u` **未转义**（同函数其它字段都过了 `escapeHtml`），而 `AttachmentUrls` 是纯客户端列表、**服务端零校验** → 任何员工可 `POST /api/Approval/submit` 存 XSS，主管/管理员点"查看详情"即在其会话执行。§10.1 声称的"6 处 XSS 已修"**漏了这处** | 服务端只接受自身生成的 `/uploads/approvals/{uid}/` 前缀；前端 `escapeHtml(u)` | `[亲验]` |
| **B17** | 工时逻辑 | `AttendanceService.cs:596` vs `:616-623` | 两份对外报表的"**旷工天数**"算法不同：月度汇总数 `Absent` 状态；模板汇总表（发工资那份）把**任何无记录的工作日**算旷工。未填 `HireDate` 的人后台永不标旷工、中途停用的人停用后无记录 → **工资表按天虚增旷工** | 两处共用同一"这天算不算旷工"判定，并补"当时是否在职" | `[亲验]` |
| **B18** | 移动端/安全 | `mobile-app/capacitor.config.json:6-7` + `Program.cs:73` | 壳以 **http** 加载已 HTTPS-only 的站点，而认证 Cookie 是 `SecurePolicy=Always` → 壳里**登录无法保持**、工号口令与人脸截帧全程明文；`android/app/src/main/assets/capacitor.config.json:6` 还存着模拟器地址 `http://10.0.2.2:50721` | `server.url` 改 https、`cleartext:false`、去掉 `usesCleartextTraffic`；打包流程固定先 `npx cap sync android` | `[亲验]` |
| **B19** | 移动端/功能不可用 | `mobile-app/android/app/src/main/AndroidManifest.xml:38-40` | 清单**只声明了 INTERNET**，而远程打卡强依赖 `getUserMedia` + `watchPosition`；Android 对"未声明的权限"运行时请求**立即拒绝且不弹窗** → **壳里远程打卡 100% 用不了** | 补 `CAMERA`、`ACCESS_FINE_LOCATION`、`ACCESS_COARSE_LOCATION`，真机验证授权流程 | `[亲验]` |
| **B20** | 明显 Bug（数据丢失） | `Services/Implementations/UserService.cs:253-257` + `AdminController.cs:532-546` | `UpdateUserAsync` **无条件覆盖** 5 个字段，而 API 的 `UpdateUserRequest` 没有它们 → 每次 `PUT /api/Admin/users/{id}` 静默清空**家庭住址/紧急联系人姓名与电话/身份证照片 URL**，并把 `AllowRemotePunch` **复位 false**。只保护了 Email | DTO 补这 5 个字段并按"非 null 才覆盖"处理；`AllowRemotePunch` 用 `bool?` 或独立端点 | `[亲验]` |
| **B21** | 安全（存储型 JS 注入） | `Pages/Admin/ShiftManage.cshtml:104,108,114`、`Pages/Admin/ZKDeviceManage.cshtml:65,69` | 自由文本（班次名/颜色/设备 SN/别名）直接拼进内联 `onclick`/`onsubmit`；Razor 只做 **HTML 编码**，浏览器**先解码实体再交给 JS**，`&#x27;` 变回 `'` 即闭合字符串 → 文员可注入、管理员点击时以管理员身份执行 | 只传 id；数据走 `data-*` + `addEventListener`；或 `@Html.Raw(JsonSerializer.Serialize(x))` | `[亲验]` |
| **B22** | 成本/性能 | `Pages/Employee/FaceEnroll.cshtml.cs:119` | 付费人脸检测**没有任何闸门**（全仓 `EnableRateLimiting` 只挂 Login/SelfRegister）→ 任何登录员工可脚本循环刷，每次 ≥1 次付费调用，且每次做 ImageSharp 解码+两次缩放+两次编码 | 复用远程打卡的冷却/每日上限，或新增限流策略 | `[引文]` |

#### 🟡 中（20 条）

| 编号 | 类别 | 位置 | 问题 | 建议修法 | 核实 |
|---|---|---|---|---|---|
| **B23** | 成本可刷 | `RemotePunch.cshtml.cs:101-115` | "补下班卡豁免"**整段跳过**唯一 30 秒成本闸门；`PunchAsync` 返回 false（未办入职/节假日）时 `ClockOutTime` 永为 null → **豁免永久成立**，可稳定刷付费接口 | 闸门无条件生效；补每日上限或端点限流 | `[引文]` |
| **B24** | 功能回归 | `Controllers/PrivateFilesController.cs:52-57` | 附件白名单含 pdf/doc/docx/xls/xlsx，但 `contentType` 只认 `.png/.webp`，其余一律按 `image/jpeg` 下发且无 `fileDownloadName` → **审批人点开 PDF/Word/Excel 是裂图** | 用 `FileExtensionContentTypeProvider`；非图片回退 `application/octet-stream` + `fileDownloadName` | `[亲验]` |
| **B25** | 定位闸门 | `AttendanceService.cs:310`+Haversine`:1845-1853` | 经纬度是 `double?` 绑表单，接受 `NaN`/`Infinity`（`1e400`）→ 距离算成 NaN → `NaN > 半径` 为 **false** → **定位校验被整体绕过** | 绑定后校验 `double.IsFinite` + 纬度 ±90/经度 ±180、Accuracy 有限且 >0 | `[引文]` |
| **B26** | 工时逻辑 | `AttendanceService.cs:601-602` vs `:815-816` | "迟到/早退次数"两套口径（按**分钟数** vs 按**状态**）→ "上午迟到 + 当天半天假"的人：模板表算 1 次、月度汇总算 0 次 | 抽共用计数方法，二选一 | `[引文]` |
| **B27** | 工时逻辑 | `AttendanceService.cs:597-598` vs `:819-822` | "缺卡次数"两套口径（模板表不看状态 vs 月度汇总排除旷工/请假/节假日/出差） | 同上 | `[引文]` |
| **B28** | 工时逻辑 | `AttendanceService.cs:635` vs `:828` | "总工时"两口径：模板表 = 正班+加班；月度汇总/我的记录 = 仅正班 → 员工看到的和工资表里的不是同一个数 | 统一口径并改列名/注释 | `[引文]` |
| **B29** | 边界 | `AttendanceService.cs:874-888` | 半天假当天"有打卡 + OnLeave"同时命中 `IsPresent` 与 `accountedForCount`，**被扣两次** → 看板"未打卡"多减 1，极端（全员半天假）**为负** | 先取并集再减：`IsPresent \|\| 状态∈{Absent,OnLeave,Holiday}` | `[引文]` |
| **B30** | 越权 | `AdminController.cs:129,178-182` | `UpdateUserRequest.ScopedDepartmentId` 默认 `null`，无法区分"未提供/清空" → 总部超管**改个手机号就把目标范围清空成"不受限"**（提权）；受限者编辑则把目标范围放大成自己的 | DTO 加 `ClearScope` 或独立 PATCH；编辑默认保留原值；校验 `requestedScope` | `[亲验]` |
| **B31** | 明显 Bug（审计） | `PunchAdjust.cshtml.cs:64-65` + `AttendanceService.cs:1285` | "补卡记录"用 `ApprovalNote.StartsWith("管理员手动补卡")` 过滤，而备注是**追加到末尾** → 这天若已有其它审批备注，手动补卡**从列表消失** | 加独立列（`ManualAdjustedAt/By`），别靠文本前缀 | `[引文]` |
| **B32** | 性能/边界 | `ShiftManage.cshtml.cs:408-411`（对比 `:172`） | 批量排班**无区间上限**（同页导出有 366 天）→ 可对几十人排几十年 ≈ 数百万实体一次 `SaveChanges`，撞 `CommandTimeout(60)` | 与导出共用"≤366 天/总量上限" | `[引文]` |
| **B33** | 可用性(DoS) | `AliyunFaceClient.cs:299-315,97,141,225` | 熔断计数是**进程级静态、跨用户共享**，且 `catch(Exception)` 把 **4xx 参数错也计入** → 任一员工连刷 5 次异常即让**全员**远程打卡失败 60 秒 | 只对 5xx/超时/限流计数；或按用户维度；打开时告警 | `[亲验]` |
| **B34** | 安全 | `Program.cs:307-334` | 异常映射把框架/EF 的 `InvalidOperationException` **原文直出浏览器**并标成 400（如 `Unable to resolve service…`） | 只放行自定义业务异常类型；其余 500 + 泛化文案 | `[引文]` |
| **B35** | 安全/启动 | `Program.cs:36-43,231-258` | `ServerVersion.AutoDetect` 首次连库异常被两个 `catch{Log.Warning}` 吞掉 → **缺连接串不 fail-fast**，进程照常 active 但全站 500 | 启动显式校验 + `Log.Fatal` 退出；或改用 `ServerVersion.Parse` | `[引文]` |
| **B36** | 安全（PII 入库） | `.gitignore:1-26`（实测缺 `PrivateUploads/`） | 敏感件已迁到 `ContentRoot/PrivateUploads`，但 `.gitignore` 只忽略了**已停用**的 `wwwroot/uploads/` → 本机跑过程序后 `git add -A` 会把**身份证照/人脸照/审批附件**提进仓库 | 加一行 `PrivateUploads/` | `[亲验]` |
| **B37** | 数据一致性 | `mobile-app/android/app/src/main/assets/capacitor.config.json:6` | 原生工程里存的是**模拟器地址** `http://10.0.2.2:50721`；直接打包必出坏包，而根配置看起来是对的，极易误判 | 打包固定先 `cap sync`；或把 `assets/` 列为构建产物 | `[亲验]` |
| **B38** | 安全 | `Program.cs`（全仓 `UseHsts`/`AddHsts`/`UseHttpsRedirection` **零命中**） | 站点靠 nginx 终结 HTTPS，但应用内**无 HSTS**，首次明文访问有降级窗口 | 在 **nginx** 加 HSTS；**不要**直接 `app.UseHttpsRedirection()`（会打断考勤机 `/iclock` 内网 HTTP 直连） | `[亲验]` |
| **B39** | 越权（批量赋值） | `AdminController.cs:232-241` | `CreateDepartment` 直接绑整个实体却**只校验 ParentId** → `AttendanceGroupId` 可指向任意考勤组（把外部组纳入自己范围），嵌套导航对象还可能被 EF 级联插入（绕过"仅总部能建考勤组"） | 改 DTO、不绑导航属性、`AttendanceGroupId` 走 `IsGroupWritableAsync` | `[引文]` |
| **B40** | 边界 | `UserService.cs:511-522` | 正则 `\d{5,}` 不限位数却用 `int.Parse` → 库里有一条 `GA`+10 位数字工号，该前缀下**自动生成工号永久 500**、不自愈 | `int.TryParse` 跳过溢出项，或限定 `\d{5,9}` | `[引文]` |
| **B41** | 边界 | `AdminController.cs:73` + `UserService.cs:99-111,236` | 工号校验/查重用 `Trim()` 后值、**入库用原值** → `" GA0001"` 绕过唯一索引且**永远登录不进去**的死账号 | 统一 Trim 后再校验/查重/入库 | `[引文]` |
| **B42** | 班次配置 | `AdminController.cs:398`（对比 `:436`） | 创建班次**不校验** `RestDaysOfWeek`（更新侧有），而 `ParseRestDays` 静默丢弃非法段 → 休息日集合为空 → **周末被当应出勤算旷工** | 抽公共校验助手，Create/Update 共用，非法 400 | `[引文]` |

#### 🟢 低（30 条，一句话/条）

| 编号 | 位置 | 问题 | 核实 |
|---|---|---|---|
| B43 | `AuthClaimsFactory.cs:26,31-36` + `AppPageModel.cs:21` | `CurrentUser` 的 Role/部门/考勤组来自 **DB（新鲜）**，但 `RealName`/`EmployeeNo` 来自 Cookie；且 `AppPageModel.CurrentGroupId` **单独读 Cookie** → `MyCalendar` 用的是**陈旧考勤组**（两套取值、新鲜度不同） | `[亲验]` |
| B44 | `UserService.cs:370-391` | 单条"启用"会补发考勤机下发，**批量启用不会** → 批量启用的人在设备上刷不了脸且零报错；批量停用还是逐条串行 | `[引文]` |
| B45 | `AdminController.cs:33-39` + `UserService.cs:428-435` | API 分页无钳制：`pageIndex=0` → `Skip(-20)`；`pageSize=2000000` → 一次拉全表（两表 join） | `[引文]` |
| B46 | `UserService.cs:611-651` | 时序防护假哈希固定 60 万次，历史账号只 1 万次（差 60 倍）→ 仍可时序筛出"老哈希账号"；且登录成功**不升级**老哈希 | `[引文]` |
| B47 | `UserService.cs:128-138` | 把所有 `DbUpdateException` 都翻译成"工号被抢先用掉"，掩盖 FK/超长/非空违规真因 | `[引文]` |
| B48 | `AdminController.cs:202,511-530` | 数值枚举不校验 → `{"role":99}` 能入库，显示成"员工"、权限全 false 的静默脏数据 | `[引文]` |
| B49 | `AdminController.cs:80,119` | 手机号未 `Trim()` 入库（页面路径会 Trim），与正常号码共存、索引对不上 | `[引文]` |
| B50 | `ExcelExportHelper.cs:306` | `dates.First()` 空集合抛异常（当前调用方保证 ≥1 天，属健壮性） | `[亲验]` |
| B51 | `ExcelExportHelper.cs:282-283` | `ExportTemplateReport` 的 `orangeOrRed/redIfPositive` 在**行循环里**调用 → 每人最多新建 7 个 style+font（2082 人 ≈ 1.4 万个样式对象） | `[亲验]` |
| B52 | `MonthlyReport.cshtml.cs:77-89` | 页面每页只显示 30 人，却每次按**全公司×全周期**现算整张模板表再内存分页 → 翻页即整份重算 | `[引文]` |
| B53 | `Dashboard.cshtml:198-201` | 下钻失败把"暂无人员"文案**永久改写**成"加载失败，请重试"，之后所有空类别都误报故障 | `[引文]` |
| B54 | `AttendanceService.cs:370` vs `:400-403` | 同一份"个人每日明细"Excel：页面导出**倒序**、API 导出**正序** | `[引文]` |
| B55 | `NotificationController.cs:19-31` | 拉未读用两次查询（列表+计数），而它每 60 秒被轮询 | `[亲验]` |
| B56 | `ApprovalController.cs:17-22` | 提交申请端点**无限流** → 可脚本批量建单，每条都给审批人发通知 | `[亲验]` |
| B57 | `ShiftManage.cshtml.cs:57,320,344` | `EarliestIn` 是死字段：弹窗无对应输入框，但每次保存把 `EarliestClockInMinutes` **静默重置为 60** | `[引文]` |
| B58 | `HolidayManage.cshtml.cs:64-65` | `Enum.TryParse` 接受数字串 → `HolidayType=99` 落库，之后被当法定节假日 | `[引文]` |
| B59 | `HolidayManage.cshtml:31-34` | 年份下拉只有"明年/今年/去年"三档，`?year=2024` 时选项全不匹配，更早年份假期**无法查看/删除** | `[引文]` |
| B60 | `HolidayManage.cshtml.cs:62-81` | 不校验 `date.Year == Year` → 在 2026 筛选下加 2030-01-01 提示"成功"但列表看不到 | `[引文]` |
| B61 | `PunchAdjust.cshtml.cs:23,39` | 日期清空时 `DateOnly` 绑定失败只写 ModelState（页面不查 IsValid）→ **静默按"今天"补卡** | `[引文]` |
| B62 | `PunchAdjust.cshtml.cs:46-55` | `DateTime.Parse`（非 TryParse）+ 兜底 catch 把英文框架原文弹给用户 | `[引文]` |
| B63 | `DepartmentManage:153,207,243`、`HolidayManage:83`、`PunchAdjust:55`、`ShiftManage:357,497`、`Publish:105` | `catch (Exception) { ErrorMessage = ex.Message }` 把 EF/MySQL 外键、字段超长、表名约束名等**内部信息显示给用户** | `[引文]` |
| B64 | `_Layout.cshtml:455,466`、`Dashboard.cshtml:31-32` | 铃铛/侧边栏遮罩/下钻卡片都是 `div + onclick`，无 `role/tabindex/keydown/aria` → 键盘与读屏用户用不了 | `[引文]` |
| B65 | `DepartmentManage.cshtml.cs:70-111` | `Rollup`/`Walk` 递归**无防环**（同项目另三处都有）→ 部门树成环时 `StackOverflowException` **整个进程崩溃**（不可捕获） | `[引文]` |
| B66 | `Program.cs:200-204` | 只放开 MVC 表单 50MB，**Kestrel 默认 30MB 仍生效** → 28.6~50MB 上传被提前拒绝，且会被异常映射成 500 | `[引文]` |
| B67 | `AttendanceBackgroundService.cs:84-88` | 清理任务**没有补跑窗口**（只有 03:00 那一分钟），与月汇总/旷工标记不对称 | `[引文]` |
| B68 | `Program.cs:190-194` | Session Cookie 只设 `HttpOnly`（未设 `SameSite`/`Secure`），且全仓**零使用** `HttpContext.Session` → 建议补 Secure 或删掉 `AddSession`/`UseSession` | `[引文]` |
| B69 | `AttendanceBackgroundService.cs:31-33,48-68` | 三个"上次执行"标记是**进程内变量**：多实例会各跑一遍（撞唯一索引报错/重复劳动）；补跑窗口只到次日 02:59，长时间停机那几天永不补标 | `[引文]` |
| B70 | `Program.cs:294-297` | `ForwardedHeaders` 未显式 `KnownProxies` → 若 nginx 不同机，按 IP 限流会退化成"全公司共用一个 20 次/分钟配额" | `[引文]` |
| B71 | `appsettings.Example.json:6` | `"DefaultPassword": "123456"` 是**死配置**（`AppSettingsOptions` 无此属性，初始密码硬编码在 `UserManage.cshtml.cs:245`/`AdminController.cs:89`） | `[引文]` |
| B72 | `Pages/Attendance/RemotePunch.cshtml:106` | 页面文案"以最后一次为准"，但实现是**上班取最早、下班取最晚** → 与实现不符（员工会误判） | `[亲验]` |

**另两条"已确认无问题、可关闭"**：`wwwroot/` 下没有任何 JS/CSS（脚本全部内联）；`escapeHtml` 全局可用性成立（`_Layout:492-586` 在 `RenderSectionAsync` 之前输出，用它的页面都要求登录）→ **§1 的 V9/B9 可判定通过**。

---

## 10.5 第 8 轮（全量通读收尾）：新增 B73~B92

> **本轮读完了此前未逐行读过的部分**：`Program.cs:1-147`、`AttendanceService.cs` 剩余段（`:248-760`、`:1060-1740`、`1740-1869`）、`ApprovalService.cs:196-753`、`UserService.cs:1-115` 与 `:542-666`、`ZKDeviceController.cs`（全部 430 行）、`ZKDeviceSyncService.cs`（全部 403 行）、`MonthlyReport`/`PunchAdjust`/`HolidayManage`/`MyCalendar`/`MyRecord`/`Board`/`Publish` 各页 code-behind、`PrivateFileStorage`/`ImageValidationHelper`、`AppSettingsOptions`/`ZKDeviceOptions`、`AttendanceDbContext` 索引区、`PunchAdjust` 的权限校验路径。
> **本轮结论：没有发现新的"高"级缺陷**；新增 **10 条中 + 10 条低**。以下引文均按"规则 1"逐字核对过当前代码。
> **未做的验证**：本轮仍是**静态阅读**——没有运行程序、没有连数据库、没有跑真机设备协议。B76/B77 两条的"运行时语义"本轮无法本地证实，已在下表中显式标注。

#### 🟡 中（10 条）

| 编号 | 类别 | 位置 | 问题（含引文） | 建议 |
|---|---|---|---|---|
| **B73** | 逻辑/工时口径 | `AttendanceService.cs:1370`（对比 `:1389`） | 上班状态基准日取**打卡当天**、而不是记录归属的 `workDate`：`var scheduled = DateOnly.FromDateTime(clockIn).ToDateTime(shift.WorkStartTime);   // 应上班时刻`；同一文件的 `CalcClockOutStatus` 已经明确改成用 `workDate` 并写了理由：`var scheduled = workDate.ToDateTime(shift.WorkEndTime);   // 应下班时刻` —— 两条对称的规则只修了一边。跨天夜班（22:00 上班）在**次日凌晨**打卡/补上班卡时，`scheduled` 被算成"次日 22:00"，`diff` 为负 → 不判迟到、`lateMinutes = 0` | 给 `CalcClockInStatus` 加 `workDate` 参数，与 `CalcClockOutStatus` 用同一套基准（`ResolveShiftTime`），调用点 `:1345`、`ZKDeviceSyncService.cs:197`、`PunchCoreAsync` 一并传 |
| **B74** | 安全/业务绕过 | `AttendanceService.cs:314-316` | 定位容差**完全由客户端提供**：`var tolerance = Math.Min(accuracyMeters ?? 0, MaxLocationAccuracyToleranceMeters);` → `if (tolerance > 0 && nearest.Distance - tolerance <= nearest.RadiusMeters)`。`accuracyMeters` 来自 `RemotePunch` 页的隐藏字段，**请求体可随意伪造**，填 100 就把"必须到岗打卡"的有效半径放宽 100 米（封顶 100 米只是限制了量级，没有解决"不该信客户端"这件事）。注释本身也承认"报的精度本身也可能不准，或者干脆被伪造" | 不采用客户端精度（改用固定小容差，或按 `RadiusMeters` 比例），并对远程打卡单独收紧；如需保留，至少记录 `LocationAbnormal` 供人工核查 |
| **B75** | 逻辑/应出勤口径 | `AttendanceService.cs:1753-1756`、`:983-986`、`ZKDeviceSyncService.cs:104-106`、`HolidayManage.cshtml.cs:40-87`、`AttendanceDbContext.cs:219` | 同一天存在"公司级"与"考勤组级"两条假期规则时，**优先级取决于数据库返回顺序**：`CountExpectedWorkdays` 用 `var holiday = holidaysInRange.FirstOrDefault(h => h.HolidayDate == d && (h.AttendanceGroupId == null || h.AttendanceGroupId == groupId));` 命中即用；而 `IsHolidayAsync`/设备同步的 `IsHoliday` 用 `Any(... != CompensatoryWorkDay ...)` —— 两条口径在"公司级法定节假日 + 考勤组级调班补班日"同日时结论相反（前者可能算出"补班、应出勤 +1"，后者认为"是节假日"）。而 `HolidayManage` 新增**没有任何重复/冲突校验**，`(HolidayDate, AttendanceGroupId)` 索引也不是唯一索引，所以这种数据能真实存在 | 明确一条优先级（建议"考勤组级 > 公司级；补班 > 休息"）并收敛到**一个**判断方法；或禁止同日同组重复配置 |
| **B76** | 逻辑（需实测） | `ZKDeviceController.cs:192-195` | 同一条 `ExecuteUpdateAsync` 里先 `SentCount + 1`、再用 `SentCount + 1` 判 `Failed`：`.SetProperty(c => c.SentCount, c => c.SentCount + 1)` … `.SetProperty(c => c.Failed, c => c.SentCount + 1 >= _opt.MaxSendAttempts)`。MySQL 单表 UPDATE 的赋值**从左到右求值、后面的表达式看到的是已更新值**，于是实际是"旧值 + 2 ≥ 5"，比设计（第 5 次才放弃）**提前一次**不再重发（`MaxSendAttempts` 默认 5，见 `ZKDeviceOptions.cs:20`）。**这条依赖数据库赋值顺序语义，本机无法证实，必须先实测** | 改成不依赖求值顺序的写法（如 `Failed = c.SentCount >= _opt.MaxSendAttempts - 1`）；实测法：造一条命令，连做 3 次心跳，看 `SentCount`/`Failed` 落库值 |
| **B77** | 正确性/重试幂等（需实测） | `AttendanceService.cs:1101`、`:1176` + `ApprovalService.cs:226-227,287` | 回写是非幂等累加：`record.OvertimeHours += approval.OvertimeDurationHours.Value;`、`record.LeaveHours += leaveHoursToday;`；而审批处理被 `var strategy = db.Database.CreateExecutionStrategy();` / `strategy.ExecuteAsync(...)` 包着（为兼容 `EnableRetryOnFailure`）。执行策略**重试时 EF 的 change tracker 不会随事务回滚还原**，`UpdateAttendanceAfterApprovalAsync` 里的 `FirstOrDefaultAsync` 会命中 tracker 中那个已经 `+=` 过的实例、不会覆盖内存值 → 理论上有一次"再累加一遍"的窗口（加班工时/请假小时数双计）。触发条件窄（必须恰在那一刻遇到可重试故障） | 回写前 `db.ChangeTracker.Clear()` 后按库值重算；或给申请单加"已回写"标记做幂等键。**先在测试库用执行策略故障注入验证是否真能复现** |
| **B78** | 性能 | `AttendanceService.cs:261` → `:782`/`:1009-1012`、`:1402` | 每次打卡都同步全量重算本人当月汇总（`await GenerateMonthlySummaryAsync(workDate.Year, workDate.Month, [userId]);`），而重算内部的"老数据补算"**逐条查库**：`if (r.OvertimeHours <= 0 && await IsNonCompRestDayAsync(db, r.WorkDate, shift, user.AttendanceGroupId))`，`IsNonCompRestDayAsync` 里每次 `var isCompDay = await db.Holidays.AnyAsync(h => ...)`；另有 `var group = groupId.HasValue ? db.AttendanceGroups.Find(groupId.Value) : null;` 是**同步阻塞查询**跑在 async 路径上。2082 人规模的早高峰打卡潮会把这两处放大成成百上千次往返 | 把当月假期/考勤组按 `:698`/`:717` 的做法**一次预载进内存**再循环；`Find` 换 `FindAsync`；补算分支加"本批已判定过 (user,date)"缓存 |
| **B79** | 安全/信息泄露 | `Program.cs:312-318` | 异常兜底把 `InvalidOperationException` 的原始消息**无条件回显**（不分环境）：`InvalidOperationException => (StatusCodes.Status400BadRequest, error.Message),`。这个类型不只服务层在抛——EF Core（如"Sequence contains more than one element"）、DI 容器（"Unable to resolve service for type ..."）抛的也是它，会把内部类型名/查询细节直接交给客户端 | 定义自己的业务异常类型（如 `BusinessException`）只回显它；其余 `InvalidOperationException` 归入 500 统一文案，细节只进日志 |
| **B80** | 安全/弱口令 | `Program.cs:275-288` | 种子管理员口令硬编码且**明文写日志**：`PasswordHash = ...UserService.HashPassword("Admin@123"),`、`Log.Information("已创建默认管理员账号：admin / Admin@123");`，且新建的种子用户**没有置 `MustChangePassword`**。触发条件是"库里一个用户都没有"，生产已有 2082 人不成立，但任何新建/演练/恢复环境一启动就带一个公开口令的 Admin | 种子口令随机生成并打印到**一次性**提示、置首登强制改密；日志不打明文口令 |
| **B81** | 安全/会话 | `Program.cs:63`（与 A8 同根因） | `options.SlidingExpiration = true;                    // 有活动就自动续期` —— 配合 A8"重置密码不吊销已签发 Cookie"，被盗会话只要**持续活动**就永不落到 8 小时上限（`TokenExpireHours` 只是"不活动超时"）。影响从"最多 8 小时"变成"无限期"。同文件 `:99-104` 的注释已经为"降权"做了 `OnValidatePrincipal`，但口令变更没有对应校验 | 与 A8 一起做：在 `OnValidatePrincipal` 里比对口令哈希/安全戳，不一致就 `RejectPrincipal()` |
| **B82** | 安全/账号锁定 DoS | `UserService.cs:62-76`、`:85` + `AppSettingsOptions.cs:14,17` | 失败计数**只在登录成功时清零**：`user.FailedLoginCount = 0;      // 登录成功，失败计数清零`。"锁定 15 分钟到期后计数仍是 5"→ 之后**任何一次**失败都会立刻再次锁定：`if (user.FailedLoginCount >= appOptions.Value.MaxFailedLoginAttempts) { user.LockedUntil = DateTime.Now.AddMinutes(...); }`。攻击者只需每 15 分钟对目标工号发一次错误口令，即可让该账号**长期无法登录**（登录 IP 限流 20 次/分钟挡不住这种低频请求）；合法用户即使输对密码，也要恰好抢在"未被再次锁定"的窗口里 | 锁定到期时重置计数（或改成"最近 N 分钟内的失败次数"滑动窗口），并让管理员可解除锁定 |

#### 🟢 低（10 条，一句话/条）

| 编号 | 位置 | 问题 | 核实 |
|---|---|---|---|
| B83 | `ZKDeviceController.cs:283` | 设备回执原文整段写日志：`logger.LogInformation("考勤机 {SN} 命令执行结果：{Result}", SN, text);` —— 同文件 `:126-127` 对未知 table 专门做了"只截前 200 字"的防日志注入处理，这里口径不一致（设备可控内容 + 可含换行） | `[亲验]` |
| B84 | `ZKDeviceController.cs:410-414` | 抓拍照片按天建目录后直接落盘，**无大小/数量上限、无清理任务**：`await System.IO.File.WriteAllBytesAsync(Path.Combine(dir, fileName), photoBytes);` —— 任一白名单 SN（或被猜到的 SN，见 A4）可反复上传撑爆磁盘 | `[亲验]` |
| B85 | `MyCalendar.cshtml.cs:31-32`、`MyRecord.cshtml.cs:21-22` + `AttendanceService.cs:845-848` | 年月只做范围兜底（2000~2100 / 1~12），而 `EnsureMonthlySummaryFreshAsync` 在**没有汇总行时直接生成**：`if (summary is null) { await GenerateMonthlySummaryAsync(year, month, [userId]); return; }` → 任一登录员工可用 URL 遍历 1200 个月，为自己插入大量 `MonthlyAttendanceSummary` 并触发 1200 次重算 | `[亲验]` |
| B86 | `ApprovalService.cs:389-400`、`:589-590` | `GetPendingForApproverAsync` 在循环里逐单 `if (!await ApproverCoversApplicantAsync(...)) continue;`，而它内部还要逐级查祖先部门（`while (cur.HasValue && ids.Add(cur.Value)) cur = await db.Departments...`）→ 待办越多、往返越多 | `[亲验]` |
| B87 | `ApprovalService.cs:497-500` vs `:452` | 建节点时取名单**不过滤在职**：`var groupApproverIds = await db.AttendanceGroupApprovers.Where(a => a.AttendanceGroupId == applicant.AttendanceGroupId).Select(a => a.UserId).ToListAsync();`，而可选名单查询是带 `a.Approver.IsActive` 的 → 提交时传入已停用审批人 id 会建出一个**登录不进来、永远处理不掉**的节点 | `[亲验]` |
| B88 | `ApprovalService.cs:478` + `AttendanceDbContext.cs:162` | 单号用 `CountAsync(...)+1` 生成，而 `RequestNo` 有唯一索引 `e.HasIndex(a => a.RequestNo).IsUnique();` → 同类型申请并发提交时后者 `DbUpdateException`（提交直接失败）；且比较用了 `a.SubmittedAt.Date`，`DATE()` 包裹导致该条件无法走索引 | `[亲验]` |
| B89 | `ApprovalService.cs:307-319` | `CancelApprovalAsync` 的两次 `ExecuteUpdateAsync`（整单置撤销 / 节点作废）**没有包在同一事务**：第二步失败时留下"单已撤销、节点仍待审批"的中间态。最终不会"复活"（`HandleApprovalAsync:275-281` 的抢占会挡住），只是审批人侧短暂看到无效待办 | `[亲验]` |
| B90 | `AttendanceService.cs:1466-1467`、`:1592-1595` | 不必要的绕：`punchTimes.Where(...).OrderBy(t => t).Cast<DateTime?>().FirstOrDefault()` 可直接返回 `DateTime?`；`Enumerable.Range(0, n).OrderBy(...).ThenBy(...).Last()` 与 `OrderByDescending(...).First()` 等价。两处都是"取最后一个/第一个"这类简单语义，改直白后 §6 的"工时计算合理"更好复核 | `[亲验]` |
| B91 | `AttendanceService.cs:1482` | `if (shift.IsCrossDay && time < shift.WorkStartTime) dt = dt.AddDays(1);` —— 跨天班次下若把午间窗口配到**早于应上班时间**（配置错误，`ShiftManage` 未校验窗口必须落在班次区间内），窗口会被整体顺延到次日 → 当天永不命中 → 视为漏打 → 有效上班时间被推到次日、工时归零。属"缺配置校验"而非算错 | `[亲验]` |
| B92 | `UserService.cs:629-651` | `VerifyPassword` 对库中哈希直接 `int.TryParse` / `Convert.FromBase64String`：哈希字符串一旦损坏（非 Base64）会抛 `FormatException` → 登录变成 500，而不是"密码错误"。与 B46 同一区域，但可独立修 | `[亲验]` |

---

## 10.6 第 8 轮引文核对结果（重要：行号漂移与一处过期描述）

按文档开头"规则 1：核对引文"，本轮把 §1/§3/§4/§6 里我读到的引文逐条对了一遍当前代码。**结论：内容全部属实，没有发现"说的和代码不一样"的实质错误；但有一处描述已被后来的修复推翻，另有多处行号漂移（改动都发生在 HEAD 之后的工作区）。**

**① 一处过期描述（需要就地加标注）**
- §3 **B2** 第 `:288` 行写："`GenerateMonthlySummaryAsync` … 被 … 调用 —— **打卡路径（`PunchCoreAsync`/设备同步）不调用它**"。**与当前代码不符**：现在 `AttendanceService.cs:261`（`PunchCoreAsync` 尾部）与 `ZKDeviceSyncService.cs:320` 都在落库后按 `[userId]` 增量刷新了。这与 §10.1 已登记"B2 已修"是一致的，但 B2 的"当前事实"段仍是修复前的口径。**处理**：保留原文但在该句后补"（**已修**，见 §10.1；现由 `AttendanceService.cs:261` 与 `ZKDeviceSyncService.cs:320` 调用）"，否则施工者读到"不调用它"会以为无事可做。

**② 行号漂移（内容一致，按方法名定位即可；建议后续引用统一带方法名）**

| 文档里写的位置 | 当前实际位置（本轮实测） |
|---|---|
| V11：`AttendanceService.cs:1060`（`OvertimeHours +=`） | `:1101` |
| V11：`ApprovalService.cs:226-232`（原子认领） | `:236-241` |
| B3：`GetMonthlySummaryAsync:374-379` | `:390-395` |
| B3：`AttendanceService.cs:817`（`SaveChangesAsync`） | `:833` |
| B3：`MyCalendar.cshtml.cs:45`（每次 GET 重算） | `:48` |
| B2：`AttendanceService.cs:1214`/`:1251`（增量刷新） | `:1254-1255` |
| C1：`ZKDeviceSyncService.cs:155`/`:234`/`:282`/`:265-270` | `:159`/`:295`/`:288`（`ResolveMidCheckResults` 调用）/`:266-268`（`groupBreaks`） |
| C1：`IsNonCompRestDayAsync` 内部 `db.Holidays.AnyAsync`「`AttendanceService.cs:966-973`」 | `:1009-1012` |
| C 类语境：`LocationAbnormalCount`「`AttendanceService.cs:848`」 | `:889` |

**③ 复核后确认"描述正确、不要改"的两条**
- **B29**（`AttendanceService.cs:874-888` 扣两次）：实测 `:888` 就是 `NotPunchedCount = userIds.Count - records.Count(IsPresent) - accountedForCount`，而 `accountedForCount` 含 `OnLeave`、`IsPresent` 只看 `ClockInTime.HasValue` → 半天假当天确实被扣两次。**引文与结论都成立。**
- **B31/B63/B66/B68/B70** 的引文逐字核对一致（`PunchAdjust.cshtml.cs:64-65`、`HolidayManage:83`、`PunchAdjust:55`、`Publish:105`、`Program.cs:200-204`、`:190-194`、`:294-297`）。

**④ 本轮"看了但判定不是缺陷"的（避免后人重复怀疑）**
- `Program.cs:294-297` 未配 `KnownProxies`：默认只信任 loopback，nginx 同机时 `X-Forwarded-For/Proto` 才生效，**外部直连无法伪造来源 IP** → 不是限流绕过（B70 说的"若不同机则退化成共享配额"仍成立）。
- `Publish.cshtml.cs:73-92`、`HolidayManage.cshtml.cs:143-150` 的"考勤组写权限"都用了 `All`（不是 `Any`），与公告撤下/查已读口径一致 → 正确。
- `PunchAdjust.cshtml.cs:43`、`OnGetRecordAsync:99`、`MonthlyReport.OnGetExportDetailAsync:184` 都做了目标员工的部门范围校验 → **手动补卡/导出明细不存在跨司 IDOR**。
- `/iclock` 的伪造打卡风险已被 `ZKDeviceSyncService.cs:65-76` 的 `UserZKDevice` 绑定校验挡住（只能给"绑过这台设备的人"写记录）→ 与 A4 描述的"心跳返回他人命令文本"是两件事，A4 仍然成立。

---

## 10.7 第 8 轮并行通读（子审查）产出与主审复核结论

> 本轮另跑了一轮**只读并行通读**，覆盖：`Pages/Admin/{ShiftManage,GroupManage,DepartmentManage,Dashboard,EmployeeInfo,ZKDeviceManage}`、`Pages/Approval/{ApplySubmit,PendingApproval}`、`Pages/Employee/{SelfRegister,FaceEnroll}`、`Pages/Account/ChangePassword`、`Pages/Login`、`Pages/Logout`、`Pages/AppPageModel`、`Pages/Attendance/RemotePunch`、`Services/Implementations/{AnnouncementService,AttendanceGroupService,EmployeeRegistrationService}`、`AttendanceSystem.Tests/*`、`Models/DTOs/*`、`Services/Interfaces/*`、`Models/Entities/*`、`Migrations/*`。
> **规则**：子审查报出的每一条"中"级结论，主审都**亲自打开代码复核**过（下表标 `[亲验]`）；未经主审复核的低级条目标 `[转述]`，**施工前必须先按规则 1 核对引文**，不要直接照改。

#### 🟡 中（3 条，主审已逐条复核）

| 编号 | 类别 | 位置 | 问题（主审复核结论） | 建议 |
|---|---|---|---|---|
| **B93** | 功能/错误文案 | `GroupManage.cshtml.cs:330`、`:335` + `AttendanceDbContext.cs:217-220` + `Migrations/20260618023257_InitialCreate.cs:124-128` | **"删除考勤组"对配过专属假期的组必然失败，而页面承诺"连带删除"并把原始英文报错抛给用户。** 复核过程：`Holiday` 的 `AttendanceGroupId` 是可空外键，但 DbContext 里**只配了索引、没配关系**：`e.HasIndex(h => new { h.HolidayDate, h.AttendanceGroupId });`；迁移里生成的外键**没有 `onDelete`**（`name: "FK_Holiday_AttendanceGroup_AttendanceGroupId", column: x => x.AttendanceGroupId, principalTable: "AttendanceGroup", principalColumn: "Id");`）→ 取 EF 对"可选关系"的默认值（数据库层 RESTRICT）。同库其余引用者：`AttendanceGroupLocation`/`ApprovalStep`/`AnnouncementRead` 都是 `Cascade`、`Department`/`User`/`ZKDevice` 是 `SetNull`，**只有 Holiday 是 Restrict**。所以只要该组挂过"仅对本组生效"的假期，`db.AttendanceGroups.Remove(g)` 就报 MySQL 1451，用户看到 `ErrorMessage = "删除失败：" + ex.Message` | 二选一：① 把 Holiday 也改成 `Cascade`（与同类一致，需迁移 + 先在测试库验 `Down()`）；② 保留 Restrict，但删除前检查并给中文提示"该考勤组还有 N 条专属假期，请先删除"。同时把 `ex.Message` 换成中文文案 |
| **B94** | 正确性/数据一致性 | `AnnouncementService.cs:16,45-46,49-63` + `Announcement.cs:16` + `Models/Entities/Holiday.cs:41,47` | **标题 197~200 字的公告会"发布失败但已落库"，变成谁都看不见的僵尸公告。** 复核过程：公告标题校验放行到 200（`:16`），而站内通知标题是 `"新公告：" + announcement.Title`（`:56`，多 4 个字），通知实体 `Notification` 的 Title 上限同为 200（**注意：`Notification` 类定义在 `Models/Entities/Holiday.cs:41`，其 `:47` 是 `[Required, MaxLength(200)] public string Title`**）→ 204 字超长，INSERT 抛 MySQL 1406；而**公告本身在 `:45-46` 已经单独 `SaveChangesAsync()` 提交**，`:63` 的第二次保存（已读名单 + 通知）**没有事务包裹**、失败不回滚 → 公告存在、`AnnouncementRead` 一条都没有。而公告栏完全由已读表驱动：`GetBoardForUserAsync` 是 `db.AnnouncementReads ... .Where(r => r.UserId == userId && r.Announcement.IsActive)`（`:133-135`）→ **所有人都看不到这条公告**，只有发布人的"我发布的"里能看到一个 0 人的公告 | 把 `:46`~`:63` 两次保存包进一个事务（`CreateExecutionStrategy` + `BeginTransactionAsync`，参考 `ApprovalService.cs:226-291` 的写法）；通知标题改成截断/短标题（如 `$"新公告：{announcement.Title[..Math.Min(80, ...)]}"`） |
| **B95** | 性能/已认证 DoS | `ShiftManage.cshtml.cs:107-109`、`:136-142`（对比 `:172`） | **展示路径的日期窗口完全没有上限，而导出路径有。** 复核过程：`ViewStart = DateOnly.TryParse(viewStart, out var vs) ? vs : today;` / `ViewEnd = ... : today.AddDays(30);` / `if (ViewEnd < ViewStart) ViewEnd = ViewStart;` —— 只有"顺序纠正"，**没有长度校验**；随后 `db.ShiftAssignments.Where(a => a.WorkDate >= ViewStart && a.WorkDate <= ViewEnd && ...)` 把窗口内**全部排班记录**一次拉进内存再 GroupBy（`:136-149`）。对照导出路径明确挡了：`if (end.DayNumber - start.DayNumber > 366) { ErrorMessage = "导出的日期范围不能超过 366 天"; ... }`（`:172`）。任何 `ManagePolicy` 账号访问 `/Admin/ShiftManage?viewStart=0001-01-01&viewEnd=9999-12-31` 即可触发全表排班加载 | 展示路径补同一道 366 天上限（与导出用同一个校验方法），超限时给中文提示并退回默认窗口 |

#### 🟢 低（12 条，`[转述]`，施工前先核对引文）

| 编号 | 位置 | 问题 | 核实 |
|---|---|---|---|
| B96 | `GroupManage.cshtml.cs:246-256`、`ShiftManage.cshtml.cs:335-353` | "更新"分支把成功提示写在 `FindAsync` 非空判断**之外** → 编辑一个已被删除的 id 时什么都没改却提示"已更新" | `[转述]` |
| B97 | `ApplySubmit.cshtml.cs:77` vs `:100`/`:146` | `Enum.TryParse<ApprovalType>(ApprovalType, ...)` **忽略大小写**，但后面 `if (ApprovalType == "Leave")` **区分大小写** → POST `ApprovalType=leave` 会跳过 `:174-183` 的午间窗口请假校验且请假字段全空（被 `ApprovalService.cs:59-60` 兜住，不写坏数据，但用户会收到"请填写起止时间"的错提示） | `[转述]` |
| B98 | `RemotePunch.cshtml.cs:177` vs `:188` | 限流表先按"人脸比对成功"记一次成功（`LogAttemptAsync(result.IsMatch, ...)`），**之后**才真正 `PunchAsync` → 打卡本身失败时，重试会被 `:110-113` 判成"刚刚已打卡成功，无需重复打卡" | `[转述]` |
| B99 | `SelfRegister.cshtml.cs:63` vs `:64-76` | 匿名可自助登记页**先把身份证照片写进 `PrivateUploads`，再做重复/格式/黑名单校验** → 校验失败时留下孤儿文件（每张≤10MB，可反复堆积） | `[转述]` |
| B100 | `ZKDeviceManage.cshtml.cs:56-58`、`:89`、`:95-96`、`:99`、`:122` | ①注释说"不受限管理员必须给设备选归属部门"，但 `var newDeptId = cu.IsScoped ? cu.ScopedDepartmentId : DepartmentId;` + 不受限恒真的 `CanAccessDeptAsync` → 总部可建出 `DepartmentId=null` 的设备；②编辑分支直接覆盖 `DepartmentId`（表单未带即清空）；③`ExecuteDeleteAsync()` 立即独立提交，与随后 `:99` 的 `SaveChangesAsync` 不同事务；④`:122` 在 `d == null` 时也提示"已删除" | `[转述]` |
| B101 | `EmployeeRegistrationService.cs:73-76` + `AttendanceDbContext.cs:226-227` | 待确认登记查重是"先查后插"，且 `Phone`/`IdNumber` 只有普通索引、**没有唯一约束** → 并发双击可产生两条 Pending，后续可能各建一个账号 | `[转述]` |
| B102 | `ShiftManage.cshtml.cs:292-305` vs `ShiftSchedule.cs:57`、`AttendanceRecord.cs:75` | 午间必打卡窗口**条数/长度无上限**，而落库列是 `[MaxLength(500)]` → 超限时抛原始 DB 异常并回显 | `[转述]` |
| B103 | `Login.cshtml.cs:67-68` | 勾不勾"记住我"，`ExpiresUtc` 都是 `AddHours(TokenExpireHours)` → **该选项实际无效**（要么让它真的区分会话/持久，要么去掉勾选框） | `[转述]` |
| B104 | `ExcelExportHelper.cs:271` + `Models/DTOs/AttendanceDTOs.cs:190` | "外出时长"列**恒空**但表头照常输出（`ExcelExportHelper.cs:211`），`OutHours` 全工程无人赋值 → 工资报表里留一列永远空的数字列，易被误填 | `[转述]` |
| B105 | `AttendanceSystem.Tests/LeaveHoursTests.cs:82`、`:146-154` | **测试质量问题（对应 §6 任务 9）**：`:82` 的输入本身就是 0，任何实现都能通过（等于没断言）；`:146-154` 名为"同一天两张假单累加"，实际是 `var accumulated = morningLeave + afternoonLeave;` **自己加自己**、没调用任何被测代码，断言与 `:122` 的 `InlineData(8,8,1)` 重复 | `[转述]`（断言内容主审未逐行复核） |
| B106 | `Migrations/20260821022210_AddCommandConfirmationAndPunchUniqueness.cs:36-43` | 迁移**先 DELETE 存量重复打卡流水、再建唯一索引**；MySQL DDL 隐式提交，若建索引失败，被删的打卡数据不可恢复，且迁移没有备份/影响行数检查 | `[转述]` |
| B107 | `Models/Entities/Holiday.cs:41` | `Notification` 实体定义在 `Holiday.cs` 里（一个文件两个不相干实体）→ 查找/维护成本高。纯组织问题，不影响运行 | `[亲验]`（文件与行号已确认） |

#### 与已有条目的关系（**不要重复开工单**）

- **"改密/管理员重置密码后旧会话不失效"** = **A8**（子审查独立撞到同一处：`Program.cs:117-128` 的 `OnValidatePrincipal` 只比角色/停用、`UserService.cs:191-194` 只改哈希、`ChangePassword.cshtml.cs:49-50` 不 `SignOutAsync`）+ **B81**（`SlidingExpiration` 把影响从 8 小时放大成无限期）→ **三处合并成 A8 一个任务**。
- **"原始异常消息回显"** 本轮又找到 3 个位置：`GroupManage.cshtml.cs:296`、`ZKDeviceManage.cshtml.cs:101`、`SelfRegister.cshtml.cs:81` → **并入 B63 的位置清单**（B63 已列 7 处）。
- **"部门树递归无环保护"**（`DepartmentManage.cshtml.cs` 的 `Rollup`/`RollupDevices`/`IsDescendantAsync`、`AnnouncementService.cs:230-235` 的受众递归）→ **并入 B65**。
- **"批量排班 / 排班导出列对齐 / 考勤组批量归组 / 远程打卡闸门 / 登记原子认领"无测试** → **并入 §6 的 E 批**（E2/E3/E8 已覆盖同类，B105 只是补充"具体哪几个分支"）。

#### 子审查提出但**未确认**的（登记为待人工确认，不要照改）

1. `GroupManage.cshtml.cs:112` 取全体审批人名单时**没有部门范围过滤**——是否真的外泄别的分公司审批人 id，取决于该视图怎么渲染（主审未读该视图渲染路径）。
2. `AnnouncementService.cs:49` `announcement.Content[..100]` 可能把 emoji 的代理对**从中间切断**（第 100 个字符正好是半个代理项时 MySqlConnector 的行为未知）。
3. `Program.cs:73` `CookieSecurePolicy.Always` 在**纯 HTTP 入口**（局域网 IP 直连、扫码用的 http 链接）下会导致 Cookie 不落地、登录静默失败——生产是否只有 HTTPS 入口，本地无法确认（**与 §7-7 的设备出口 IP 清单一起确认**）。
4. `ZKDeviceManage` 允许总部建 `DepartmentId=null` 的设备后，是否真的没有任何界面能再管到它。

---

## 11. 实施回执表（施工者逐条填写）

> **本表已按第 9 轮整理精简**：所有**已修复/已完成/已作废**的任务行（V1~V12、A5、A6、A9、B1~B6、B8~B14、C3、D1 共 **29 行**）**已删除**，只保留仍需施工的 **26 行**（A1~A4、A7、A8、A10、A11、B7、B8、C1、C2、C4~C7、E1~E10）。已删任务的证据见 §0.7 的提交对照表与 §10.1。
> **尚未纳入本表**：§7 的 9 项人工确认、§8 剩余的 8 项待决策、以及 §10.4/§10.5/§10.7 的 **93 条**新增问题（B15~B107，是否升级为正式任务见 §11.1）。

| 任务编号 | 状态 | 改动文件（含行/方法） | 自测方式与结果 | 备注 / 遗留 |
|---|---|---|---|---|
| A1 | ⬜ 未开始 | | | |
| A2 | ⬜ 未开始 | | | |
| A3 | ⬜ 未开始 | | | |
| A4 | ⬜ 未开始 | | | |
| A7 | ⬜ 未开始 | | | |
| A8 | ⬜ 未开始 | | | |
| A10 | ⬜ 未开始 | | | |
| A11 | ⬜ 未开始 | | | |
| B7 | ⬜ 未开始 | | | |
| B8 | ⬜ 未开始 | | | 仅剩"管理范围判定收敛"（格式校验已完成） |
| C1 | ⬜ 未开始 | | | |
| C2 | ⬜ 未开始 | | | |
| C4 | ⬜ 未开始 | | | |
| C5 | ⬜ 未开始 | | | |
| C6 | ⬜ 未开始 | | | |
| C7 | ⬜ 未开始 | | | |
| E1 | ⬜ 未开始 | | | |
| E2 | ⬜ 未开始 | | | |
| E3 | ⬜ 未开始 | | | |
| E4 | ⬜ 未开始 | | | |
| E5 | ⬜ 未开始 | | | |
| E6 | ⬜ 未开始 | | | |
| E7 | ⬜ 未开始 | | | |
| E8 | ⬜ 未开始 | | | |
| E9 | ⬜ 未开始 | | | |
| E10 | ⬜ 未开始 | | | |

**状态取值**：⬜ 未开始 ｜ 🟡 进行中 ｜ ✅ 已完成（附自测证据）｜ ⏭️ 不适用（附理由）｜ ❓ 需人工确认（附卡点）｜ 🟥 **引文不符**（附你实际看到的代码）

**填表要求**：
- "改动文件"必须写到**方法名**（行号会漂移），新增文件写全路径
- "自测方式"必须能复现：写了什么测试 / 跑了什么命令 / 手工怎么点
- 涉及数据库的，注明迁移文件名与是否在测试库跑过 `Down()`
- 有用户可感知改动的，注明是否已更新 `docs/更新日志.md` 并跑过 docx 同步脚本
- **任何"引文不符"都必须登记**（这是发现文档过期的唯一途径，不要默默跳过）

---

### 11.1 关于 B15~B107（尚未派生为可施工任务）

第 9 轮整理后，本表的正式任务为 **26 条**：**A1~A4、A7、A8、A10、A11、B7、B8、C1、C2、C4~C7、E1~E10**（原 55 条中已修复/完成的 29 条已删除，见 §0.7 与 §10.1）。**B15~B107（§10.4 / §10.5 / §10.7，共 93 条）目前仍只是"已登记的问题清单"**，还没有转成带验收标准的施工任务。

在你决定之前，施工者**不要**自行把它们当任务开工。需要你定的是：

1. 这 **93 条**（B15~B107）里，哪些要**升级成正式任务**（升级 = 我在 §3/§4 补一段"位置 + 引文 + 问题 + 要改成 + 验收"，并在本表加上对应回执行）？
2. 建议的优先级（我的排序意见）：
   - **先做**：B15（无范围 Clerk 被当总部超管，`DeptScopeService.cs:58-68` 无角色检查；同类口径还出现在 `ApprovalService.cs:573-574` 的兜底审批人选取）、B82（账号锁定 DoS）、B74（定位精度可伪造）、B93（删考勤组必失败 + 英文报错外泄）。
   - **随 A8 一起做**：B81（旧会话在改密后仍无限续期）。
   - **随 §8-8（历史月份重算口径）一起决策**：B75（同日多条假期规则的优先级）。
   - **随 C 批性能任务一起做**：B78、B86。
   - **可以最后做**：其余低条目（文案/一致性/组织）。
3. **B76 / B77 必须先实测再决定改法**（依赖数据库赋值顺序语义 / EF 执行策略重试下的 change tracker 行为），不要凭文档直接改。
